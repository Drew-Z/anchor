# Current Development State

Updated: 2026-09-15. This is the current execution pointer, not a release approval.

## Development Mode

- Codex owns the complete development loop, including UI implementation and emulator verification, under `../AGENTS.md`.
- Canonical checkout: `D:\workspace4Cursor\learn\anchor`, branch `codex/anchor-web-demo`.
- Transition baseline: `173a6c9a039fba27f8ad439a870f6306f66bc0f2`.
- Workflow migration commit: `54ab396` (`docs: switch Anchor development to Codex only`). Current code is the canonical branch HEAD; the transition baseline is not a reset target.
- Continued through tasks `01a01e2e-d37b-7760-8934-ae0b4dd72be9`, `01a07cfd-0714-73c0-bc13-a10f2bdf8784`, `01a07de8-546a-7062-8a37-c4f5048298ce`, `01a07e0b-378b-78a2-9041-605bca5892b6`, `01a081db-b31d-7693-8ecb-6040d5d44852` and `01a09011-4502-77b2-9078-2187e0e41efa`. Current task: `01a09531-76ee-7b91-839d-0daa7fe4c68e`, resumed at `391cd81` with the unfinished DL-024 changes and protected reports retained. This task completed DL-024 at `414e846`, DL-025 at `39fcc57`, DL-026 at `5448f8e`, DL-027 at `7a022c2` and DL-028 at `fa3f274`, each with local/Android acceptance, ordinary-APK restoration and temporary cleanup. The leaf records and post-commit root reassessments are below.
- The old `anchor-web-productization-loop` automation was removed on 2026-09-05. A Codex-only heartbeat was enabled on 2026-09-08 after two manually completed leaves. The user subsequently cancelled automatic wake-ups; development now continues through direct task messages.
- Claude task `20260905-135936-e5d2eef4` failed before editing. Its initial agent and both recovery sessions have been stopped. Its provider failures and exhausted retries are historical evidence, not a current development blocker or a task to resume.

## Continuous Development Pointer

- Root task: `anchor-development-loop`, defined in [DEVELOPMENT_LOOP.md](DEVELOPMENT_LOOP.md). This section is the single execution pointer.
- Round: 29. Phase: `waiting_external`. Active leaf: none. DL-001 through DL-029 are complete. DL-029 closes the bulk verification stale-cache boundary after route exit; the application commit and required local/Android acceptance, screenshot review, restoration, shutdown and cleanup are complete. The root reassessment found no additional actionable local work; publication authorization, fresh release acceptance and formal cohort evidence remain external.
- Latest application verification: DL-029 at `b903c2a`; 95 focused bulk/single/library/search checks and 654 final full Flutter tests pass. Final analysis found no issues (63.8 seconds), three Dart files format with zero changes, and the application diff check passes. Android normal/short-large-text acceptance has 32 observations and 24 screenshots, all visually inspected. Evidence: `D:\Agent\codex\visualizations\2026\09\12\01a09531-76ee-7b91-839d-0daa7fe4c68e\anchor-dl029-20260914-evidence.json`. The current Web Demo verification adds 75 Node unit tests and 93 Playwright E2E tests, all passing. Ordinary APK/display/database restoration and emulator shutdown are complete. These are local synthetic-fixture results, not remote CI, live-model, physical-device or release approval; coverage was not remeasured.
- Current assessment: DL-029 closes the reproduced bulk-save freshness boundary. The current signed release candidate, physical-device smoke and Windhub primary/fallback five-case model acceptance are recorded in the local readiness evidence. The formal cohort study is no longer a release gate; the readiness evaluator now returns `GO` for the technical gates. Distribution and publication authorization remain separate external actions; cancelled automatic wake-ups are unchanged.

### Active Leaf DL-029 - Bulk verification save completion

- Baseline: `b54621d2d507f8a1fc721c223bc57d04a1c0949f`, clean tracked files, 94 commits ahead and zero behind the locally cached feature remote. Both protected-report hashes match; they and the unowned `%SystemDrive%` directory remain untracked. DL-028 restoration and shutdown remain complete.
- Goal/output: determine whether a confirmed batch that finishes after leaving the library leaves stale pending/all-question state on reopening or in a surviving consumer. If reproduced, successful completion must refresh the existing shared caches while their provider scope survives; failed writes preserve prior state, and a disposed scope performs no later cache/UI work. The already-started atomic batch may settle once.
- Inputs: `_PendingQuestionsTabState._verifyAll` returns after `updateQuestions` when unmounted, before its current cache refreshes. Existing bulk widget tests verify settlement but not reopened-list freshness. `QuestionVerificationOperations` supplies the accepted shared-provider lifetime pattern; bulk service/transaction tests and Trellis 10.3/10.30/10.41/12.17 preserve verification, normalization and search contracts.
- Owned files: `lib/core/providers/providers.dart`, `lib/features/knowledge_base/knowledge_base_screen.dart`, `test/question_bulk_verification_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary helpers use `anchor-dl029-20260914-01a09531-` in the system temporary directory; retained evidence uses `anchor-dl029-20260914-` in this task's existing visualization directory.
- Acceptance: before production edits, use the real library and controlled synthetic batch writes to check successful/failed completion after route exit and reopening at ordinary/short-200% sizes. Cover surviving cache consumers, existing list/search/practice/learning-plan/deck/point/old-new citation targets, unrelated-cache isolation and started-write completion after provider-scope disposal. Preserve read-exit cancellation, readable confirmation, cancellation without writes, busy ownership, mounted retry and batch atomicity. If reproduced, run focused bulk/single/service/transaction/library/search checks, the full Flutter suite, analysis, formatting and diff checks. Verify ordinary and short/200% Android tap/scroll/confirm/Back/reopen flows with synthetic repositories and blocked database/model access; inspect screenshots, restore the ordinary APK/display/database baseline, stop the emulator and clean only this leaf's temporary files before local commits.
- Boundary: no bulk planning or citation/status semantics, repository/database/transaction, confirmation layout/text, navigation, single-save behavior, cache policy redesign, model, physical-device/release/cohort or automation changes. This leaf investigates batch completion independently; DL-028 does not establish its reproduction.
- Status: `completed`; local commit: `b903c2a` (`fix(library): refresh bulk verification after route exit (DL-029)`). Final local gates, Android run3, visual review, ordinary restoration, shutdown, cleanup and scoped application review are complete. The immediate root reassessment follows below.
- Reproduction: before production edits, the corrected baseline passed 15 and failed both successful-reopen cases: the batch had completed once, but the actual list had no `已核验 1` filter (`anchor-dl029-20260914-baseline2.log`, exit 1). Failure/reopening, scope-disposal settlement and previous bulk contracts passed. A second independent cache baseline passed mounted refresh and failed the surviving consumer after Back: it retained `Verified 0, pending 2` rather than `Verified 1, pending 1` (`anchor-dl029-20260914-cache-baseline.log`, exit 1).
- Fixture correction: the first baseline passed 13 cases and stopped all four new reopening cases at a test-only `pageBack` assumption because the library has no page back button. The fixture now sends the platform Back route event; production and expected cache behavior were unchanged. This first attempt is retained separately and is not defect reproduction.
- Implementation: `QuestionVerificationOperations.saveBulk` owns the existing single batch update and exact bulk cache targets, including learning-plan families and old/new citation/deck/point caches. Scope disposal prevents later refresh; route disposal alone does not. The screen retains planning, confirmation, pre-write lifetime guards, busy state, errors and success feedback. Individual saving is unchanged.
- Local checks: 95 focused bulk/single/service/transaction/library/search checks and 654 full Flutter tests pass, exit 0; full test output reports 39 seconds. Analysis found no issues (63.8 seconds), three Dart files format with zero changes, and the diff check passes. Logs use `anchor-dl029-20260914-`, with `focused2.log` and `local-checks.json` identifying the final gates. The first focused command passed 73 cases but named a nonexistent search-test path; it exited 1 and is retained as a command-selection failure. Correcting that path changed no code or expected behavior.
- Android preparation: the existing `xunqiu64_api36` AVD cold-booted in 22.1 seconds. The installed DL-028 ordinary APK and database match baseline; no registration recovery or ordinary app launch occurred. Original display is 1080x2400/density 420/font 1.0. A temporary synthetic fixture using the actual library and providers was used for acceptance.
- Android progress: the fixture build passed (83.6 seconds) with the existing SDK XML warning. APK extension/entrypoint and accepted source/test hashes were checked; fixture installation preserved the database baseline. Run1 completed 16 ordinary-screen observations, then stopped before the first short-screen confirmation; it retained 13 screenshots including the failure image. No app-PID framework error was logged, and display/forward restoration succeeded. The failure image shows the bulk action below the visible content; the controller now requires a sufficiently visible tap area above the system navigation region and scrolls before tapping. This helper-only correction changes no application or acceptance expectation; run2 is pending. The native emulator child is `qemu-system-x86_64-headless.exe` (PID 26796, parent launcher 9696), recorded separately because the startup filter used the non-headless filename.
- Android continuation: run2 again completed all 16 ordinary observations and opened the short-screen confirmation, but stopped because its controller required the first body glyph to be readable before scrolling past the title. The failure screenshot shows the scrollable title and partially clipped first body line with reachable action buttons; no framework error was logged. The fixture now reports the dialog's full scroll viewport, and the controller scrolls to both the beginning and ending before asserting readability. These are helper-only corrections; application/test hashes and the passed local gates remain unchanged. Both failed runs restored display/forward state and stopped their fixture app. The ordinary `lib/main.dart` build passed (19.0 seconds), its kernel has no DL fixture markers, and a verified temporary copy is retained so the fixture-only rebuild cannot replace the restoration artifact.
- Android run3: the fixture-only rebuild passed (38.1 seconds); application/test hashes remain identical to the passed local gates. Run3 exited 0 with 32 observations and 24 screenshots across ordinary and short/200% settings. Every observation has zero framework errors, blocked database opens and model accesses; the filtered app-PID log and restorationErrors are empty. All 24 screenshots were individually inspected and accepted; short-screen confirmation edges were read through physical scrolling, while existing filter clipping, row-preview ellipsis, offscreen content and transient snackbar overlays remain accurately recorded. Ordinary APK restoration, final database/process checks and display restoration match baseline; the emulator and controller were stopped, and five leaf-created temporary files were removed.
- Evidence: `anchor-dl029-20260914-evidence.json` (SHA-256 `1cc8c93f5cb353498675a4e771921f18b07975bc2b25c3ae279082ceb5dcb0d9`), visual review `anchor-dl029-20260914-visual-review.json` (SHA-256 `eb8e8cc8c16b28a7b8660ca27081fc69bcfecbb0c8ef852aaefb699552565a3c`), ordinary restoration `anchor-dl029-20260914-restoration.json`, shutdown `anchor-dl029-20260914-shutdown.json`, and cleanup `anchor-dl029-20260914-cleanup.json`. The final ordinary APK SHA-256 is `b93165936ebade6a6287eaf67ca9afe030fda250451feaf88404b99b130061fe`; database SHA-256 remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Five explicitly created Temp files were hash-recorded and removed; protected reports, `%SystemDrive%`, prior helpers/APKs, historical worktrees and caches remain untouched.

### Completed Leaf DL-028 - Individual verification save completion

- Baseline: `0ded0b338e22046bf150565f2e0be670325355c7`, clean tracked files, 92 commits ahead of the locally cached feature remote. Protected-report hashes match; both reports and the unowned `%SystemDrive%` remain untracked. No matching task runtime or ADB device is active.
- Goal/output: a successful individual question-status write refreshes existing shared question/citation/search/practice caches even if the detail route has closed. A failed write keeps the original state, and mounted failure remains retryable. A disposed provider scope performs no subsequent cache/UI work; a started write may settle once. Busy actions cannot create duplicate writes.
- Inputs: `QuestionEvidenceScreen._setStatus`, `questionRepositoryProvider`, citation/status read models and their learning-plan dependencies, existing bulk/library tests, and the provider-owned lifetime pattern in `LocalDataOperations`. Trellis leaves 10.3, 10.30, 10.41 and 12.17 require post-save list/search freshness and existing citation normalization.
- Owned files: `lib/features/knowledge_base/knowledge_base_screen.dart`, `lib/core/providers/providers.dart`, new `test/question_verification_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary helpers use `anchor-dl028-20260913-01a09531-` in the system temporary directory; retained evidence uses `anchor-dl028-20260913-` in this task's existing visualization directory.
- Acceptance: first reproduce against unchanged application code using a controlled write, real detail navigation and a surviving library provider scope. Check successful/failed completion after back or scope disposal, mounted retries, busy ownership, verified/pending/no-source normalization, old/new citation keys and relevant existing cached read models. If a user-visible stale state is reproduced, move only individual save completion to a provider-owned lifetime. Run focused single/bulk/library/search and relevant provider regressions, full Flutter tests, analysis, formatting and diff checks. Exercise ordinary and short/200% Android tap/scroll/back/save flows with synthetic repositories, blocked database/model access and screenshots; restore ordinary APK/display/database, stop the emulator and clean only leaf-created temporary helpers before local commits.
- Boundary: preserve source-status/citation semantics, database/repository behavior, existing cache invalidation targets, layout/text, bulk verification, navigation availability and learning-plan dependencies. Do not extend to general mutation/cache architecture, model calls, physical-device/release/cohort or automation work.
- Status: `completed`; local commit: `fa3f274` (`fix(library): refresh single verification after route exit (DL-028)`). Required local/Android gates, visual review, ordinary restoration, shutdown, cleanup, final local review/commit and the immediate root reassessment are complete. The auxiliary read-only review was interrupted without a result and is not counted as verification.
- Reproduction: unchanged production passed 12 cases and failed both successful-save-after-back cases at ordinary and short/200% sizes (`anchor-dl028-20260913-baseline.log`, exit 1). Persistence completed once but the library retained pending state. The additional cached-read-model baseline passed mounted completion and failed after back because the verified-question cache remained empty (`anchor-dl028-20260913-cache-baseline.log`, exit 1). No application changes preceded either baseline.
- Implementation: a provider-owned `QuestionVerificationOperations` performs the unchanged single repository update and refreshes the existing cache targets only while its scope remains alive. Existing unread caches are not initialized as an invalidation side effect. The detail screen keeps normalization, busy state and feedback, checking its own lifetime before entry and after await. No layout, text, bulk or repository behavior changed.
- Local checks: 87 focused checks passed, including 16 single-save cases. Initial full verification passed 646 tests but analysis reported one new test-only string-interpolation info. After that cosmetic assertion correction, the final full suite passed 646 tests (47 seconds), analysis found no issues (13.0 seconds), and three Dart files format with zero changes, all exit 0 (`anchor-dl028-20260913-final3-*`). The earlier diff check passed. The final application diff is unchanged since focused verification.
- Android preparation: auxiliary Node-launched PowerShell processes had an incomplete native-command environment. The first follow-up check did not execute (only an empty format log) and the next stopped before tests with a missing Windows environment variable; neither is a passed check. The first emulator attempt failed before launch; the next cold boot timed out and left no live target. Using the established native PowerShell tool environment, startup3 completed in 46.9 seconds without registration recovery, with the accepted DL-027 ordinary APK and unchanged database hash. No ordinary app was launched. The final fixture and UI results are recorded below.
- Android acceptance: the synthetic fixture build passed, exit 0. Controller run1 stopped before app launch on an AVD-name CRLF comparison (zero observations/screenshots); the helper-only correction changed no app source or acceptance expectation. Run2 passed with 28 observations and 24 screenshots at 1080x2400/1.0 and 840x1100/2.0 on API 36 / `xunqiu64_api36`, density 420. Physical taps/swipes/Back covered busy duplicate prevention, return-before-save, successful late refresh/reopening, late failure, mounted failure/retry and verified/pending/no-source decisions. Missing citations cannot verify; selecting pending with no readable citation preserves the existing no-source fallback. Scope disposal and controlled write completion use the fixture extension. All observations have zero framework errors, database opens and model accesses; the filtered app-PID log is empty and controller restorationErrors is empty.
- Visual review: all 24 run2 images were individually inspected and accepted in `anchor-dl028-20260913-visual-review.json`. Existing short-screen scroll clipping, row-preview ellipsis and temporary snackbar overlays are unchanged; physical scrolling and feedback expiry keep subsequent controls usable. These images do not claim that all content is simultaneously visible.
- Restoration: ordinary `lib/main.dart` build passed, exit 0. APK SHA-256 is `35b74150116c3acb846126c293776d5ce5db0ca4e16542aba1f32985ac03365b`; its installed hash matches, the kernel has no DL fixture markers, and application/test hashes match the accepted fixture. The ordinary app was not launched. Database SHA-256 throughout startup, fixture installation, acceptance and ordinary restoration remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display is restored to 1080x2400/density 420/font 1.0. No app PID, target forward, emulator or controller remains; shared services were retained.
- Evidence/cleanup: `anchor-dl028-20260913-evidence.json`, SHA-256 `19c4feb6e13fac44a9489200f0916d214f339d0b91da6e89b6f8225d781e9400`, retains 58 artifact hashes plus source/helper/APK hashes, baseline and preparation classifications, final local/Android gates, visual review and restoration/shutdown records. Eight explicitly created Temp helpers/logs were hash-recorded and removed. Two additional Flutter boolean-marker files, `.flutter` and `.flutter_tool_state`, were traced to the auxiliary process missing APPDATA using clean-entry evidence, their exact contents and the SDK fallback-to-current-directory code; their hashes and attribution are recorded, the workspace audit ran, and only those two new files were removed. Ten leaf-created temporary files were removed in total. Both protected-report hashes match; those reports, `%SystemDrive%`, prior task helpers/backups, historical worktrees and caches remain untouched.
- Final review: the primary agent inspected the complete new widget test and the final application diff, then rechecked the accepted source/test hashes, all 24 screenshot hashes, ordinary APK, restoration/shutdown records, absence of all ten removed temporary files and protected-report hashes. No additional issue was found within this single-save scope. The index was completed after interruption recovery; no accepted application/test file changed and no passed behavioral gate was rerun.

### Completed Leaf DL-027 - Bulk verification lifetime and confirmation reachability

- Baseline: `014de0ce38ac8a5f96b09491ba871337a50adf0a`, clean tracked files. Both protected-report hashes match; they and the unowned `%SystemDrive%` directory remain untracked. No task runtime or ADB device is active.
- Goal/output: leaving the pending-question page or disposing its provider scope while a citation read is pending prevents subsequent citation repository reads and prevents confirmation, new writes and post-disposal UI/provider work. An already-started write may settle once. Mounted read/write failures remain retryable, cancellation performs no write, and confirmed success retains the existing update plan and refresh behavior.
- Inputs: `_PendingQuestionsTabState._verifyAll`, `QuestionBulkVerificationService.buildPlanFromLoader`, question/source-chunk repositories and the existing service/transaction tests. The service awaits each distinct citation in sequence; the baseline screen checked `mounted` only after the complete plan, and after its post-write provider invalidations.
- Owned files: `lib/features/knowledge_base/knowledge_base_screen.dart`, new `test/question_bulk_verification_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary helpers use `anchor-dl027-20260913-01a09531-` in the system temporary directory; retained evidence uses `anchor-dl027-20260913-` under this task's existing visualization directory.
- Acceptance: first use delayed synthetic citation reads and writes against unchanged production code to cover route exit, provider-scope disposal, late success/failure, busy-action ownership, no eligible citations, confirmation cancellation, mounted failures/retry and confirmed success. If reproduced, correct only the screen-owned lifetime boundaries. Run bulk service/transaction/widget and library/search checks, full Flutter tests, analysis, formatting and diff checks. Exercise actual Android pending/error/retry/cancel/confirm/back flows at ordinary and short/200% settings with synthetic repositories and blocked database/model access; inspect screenshots, restore the ordinary APK/display/database baseline, stop the emulator and remove only leaf-created temporary helpers before local commits.
- Boundary: no service planning or cancellation API, citation normalization, status semantics, repository/transaction changes, individual question-status save, cache policy redesign, layout redesign, model, release or automation work. Retain the existing atomic batch update and its mounted refresh set.
- Status: `completed`; local commit: `7a022c2` (`fix(library): guard bulk verification and scroll confirmation (DL-027)`). Required acceptance, restoration, cleanup and the scoped local commit are complete. The contract preceded regression and production edits. The valid baseline passed seven cases and failed both late-success route/scope-exit cases: observed citation calls were `['first', 'missing']` instead of `['first']` (`anchor-dl027-20260913-baseline3.log`, exit 1). The first attempt had a test-only provider-name compile error; the second combined these two real failures with test-only waits for a continuous progress animation. Correcting those fixture mistakes changed no production code or acceptance expectation; earlier attempts remain retained.
- Implementation: check lifetime at bulk-action entry, before each citation repository read and immediately after the already-started batch write settles. Existing service planning, normalized updates, confirmation text/decisions, cancellation, error messages, busy state and mounted provider refreshes are unchanged. The confirmation title and explanation share a scrollable area under the narrow amendment below.
- Pre-amendment local checks: 69 focused bulk service/transaction/widget and library/search checks passed, including the original nine new widget cases. All 628 full Flutter tests passed (39 seconds); analysis found no issues (68.4 seconds); both changed Dart files format with zero changes and the diff check passed. All commands exited 0. These results predate the confirmation-layout correction and are retained separately from the refreshed final gates below.
- Android preparation: the first startup check stopped at its 60-second deadline; the same AVD finished cold boot in 64.9 seconds and was reused. The prior DL-026 ordinary APK was still registered with a matching installed hash, the database matched baseline, and the ordinary app was not launched. The first temporary-fixture build failed on incomplete privacy-store methods and a render-viewport type check; helper-only corrections made build2 pass. Its extension/entrypoint and source/helper/APK hashes were verified before installation. The first ordinary-entrypoint build also completed with exit 0; it predates the confirmation amendment and is not the final restoration artifact.
- Android run1: exit 0 with 26 lifecycle observations and 18 screenshots across normal and short/200% settings. All observations have zero framework errors, blocked database opens and model/executor accesses; the filtered app-PID log is empty and the controller restored display settings and its forward. All screenshots were visually inspected. The short `retry-confirmation` image shows both action buttons but no readable explanation; successful automated lifecycle assertions do not constitute full visual acceptance. This pre-amendment run remains retained; final run2 covers the correction.
- Confirmation contract amendment: within the same owned screen and widget-test file, make the unchanged confirmation title and full explanation reachable by scrolling at ordinary and short/200% sizes, while retaining reachable cancel/confirm actions. Reproduce first on the unchanged dialog; cover reading the beginning/end, cancellation without writes and the same confirmed normalized update. Preserve all text, plan/citation/status semantics, atomic writes, theme and other layouts. Refresh focused/full/analysis/format/diff gates and both Android configurations, adding physical scrolling and screenshots of the explanation before final restoration and cleanup. This is a narrow exception to the original layout exclusion, recorded before the new tests or dialog edit.
- Confirmation reproduction/correction: the unchanged dialog passed ordinary-size reading/cancel/confirm and failed short/200% reading in `anchor-dl027-20260913-dialog-baseline2.log` (one pass, one failure, exit 1). The final character remained at y=356-388 while the readable content area ended at y=212, even after bounded drag attempts. The first attempt had a test-only gesture-API compile error and is retained separately. The existing AlertDialog now scrolls its title and explanation together; strings, action callbacks and all bulk semantics are unchanged. The original run1 visual record is `anchor-dl027-20260913-visual-review1.json`; it is not final acceptance.
- Refreshed local checks: 71 focused bulk service/transaction/widget and library/search checks passed, including all 11 bulk widget cases. All 630 full Flutter tests passed (41 seconds); analysis found no issues (70.8 seconds); both changed Dart files format with zero changes and the diff check passes. All commands exited 0; logs use `anchor-dl027-20260913-final-`. Final fixture build3 and ordinary-entrypoint build2 also passed, exit 0. Production/test hashes remain those accepted by these gates; earlier 69/628 results are pre-amendment evidence.
- Final Android acceptance: run2 exited 0 with 30 observations and 22 screenshots on Android API 36 / `xunqiu64_api36`, density 420, at 1080x2400/1.0 and 840x1100/2.0. Physical tap/swipe/Back verified busy-action ownership, pending-read exits and late results, mounted read failure/retry, readable confirmation beginning/end, cancellation, no readable citations, mounted write failure/retry and already-started write settlement after exit. Provider-scope disposal and controlled read/write completion use the fixture extension, not user buttons. All observations have zero framework errors, blocked database opens and model/executor accesses; the filtered app-PID log is empty. All 22 screenshots were visually inspected. Mounted successful writes refresh the two watched question lists once; cancelled, failed and exited operations retain their original list-read counts. All repository changes are synthetic memory data.
- Final restoration: ordinary APK SHA-256 is `a533131dc0b6aacd69163e88b9c3da3ef0d2614199b5bf8ae8e7718c79aaa0dc`; the installed hash matches, the kernel has no DL fixture extension/entrypoint markers, and source/test hashes match the accepted fixture. The ordinary app was not launched. Database SHA-256 before acceptance, before restoration and after installation remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display returned to 1080x2400/density 420/font scale 1.0; no forward or app PID remained. The emulator and controller are stopped, ADB lists no devices and shared services were retained.
- Evidence/cleanup: `anchor-dl027-20260913-evidence.json` in the evidence directory above, SHA-256 `4add6090ca9fcdb12e535c6088b449f2169f31facb672b6258e5fb0992405daa`, records 78 retained artifact hashes, source/APK hashes, attempt classifications, visual review, restoration, shutdown and cleanup. Exactly four newly created temporary files with prefix `anchor-dl027-20260913-01a09531-` were hash-recorded and removed: fixture source, device controller and emulator output/error logs. All original acceptance logs/JSON and both runs' screenshots remain evidence. Both protected reports retain their baseline hashes; they, `%SystemDrive%`, previous-task helpers/backups, historical worktrees and caches remain untouched.

### Completed Leaf DL-026 - Populated question-filter reachability

- Baseline: `5338b8c2d3700aec6a59b5b824507b639a11d2c2`, clean tracked files. Both protected report hashes match; the reports and unowned `%SystemDrive%` remain untracked. No task runtime or ADB device is active.
- Goal/output: populated question lists expose all source-status filters, matching question rows and zero-match recovery without overflow at ordinary and short/200% sizes. Direct entry and entry from another tab work with the metric header initially expanded; returning from question evidence retains the selected filter and original question identity.
- Inputs: `_QuestionsTabState.build` renders `_QuestionStatusFilters` above an `Expanded` list in a fixed-height Column, unlike adjacent scrolling list bodies. `_QuestionStatusChip`, `QuestionEvidenceScreen` navigation and the existing library layout regression fixture supply the comparison and acceptance surface.
- Owned files: `lib/features/knowledge_base/knowledge_base_screen.dart`, `test/knowledge_library_layout_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary helpers use `anchor-dl026-20260913-01a09531-` in the system temporary directory; retained evidence uses `anchor-dl026-20260913-` in this task's existing visualization directory.
- Acceptance: first run controlled nonempty-list checks on unchanged production code at 390x844/1.0 and 320x420/2.0. Verify direct and cross-tab entry, all three source statuses and counts, zero matches and recovery, original selected-question navigation, retained selection after back and unchanged provider read counts. If reproduced, correct only the scrolling composition and run focused library/search/Agent checks, full Flutter tests, analysis, format and diff checks. Exercise physical Android scrolling/filtering/navigation at ordinary and short/200% settings with synthetic providers and blocked database/model access; inspect screenshots, restore the ordinary APK/display/database baseline, stop the emulator and remove only this leaf's temporary material before local commits.
- Boundary: no filter semantics, status/citation normalization, question row text/preview redesign, bulk verification, question status writes, provider/repository protocol, persistence, model, release or automation changes. Existing empty/error and search behavior must retain their accepted contracts.
- Status: `completed`; local commit: `5448f8e` (`fix(library): keep question filters reachable (DL-026)`). Required acceptance, restoration, cleanup and the scoped local commit are complete. The contract preceded test and production edits. The unchanged production screen passed all three ordinary cases and failed all three short/200% cases with a 28-pixel bottom overflow, including direct and cross-tab entry (`anchor-dl026-20260913-baseline.log`, exit 1). The existing filter/count/navigation assertions already passed in ordinary size.
- Implementation: the populated question tab now places the existing status filters and separated rows or zero-match state in one coordinated sliver viewport. Selection, counts, row content, spacing and navigation callbacks are unchanged.
- Local checks: 80 focused library/search/Agent checks and 619 full Flutter tests passed, exit 0, including the six new regressions. Analysis found no issues (70.7 seconds); both changed Dart files format with zero changes and `git diff --check` passes. Logs use `anchor-dl026-20260913-` in the current evidence directory. Production and test files have not changed since these gates.
- Android preparation: the cold boot again reported an unknown Anchor package. Reinstalling the verified previous DL-025 ordinary APK without launching it restored package visibility; its installed hash matched and the database remained the recorded baseline. The DL-026 synthetic fixture and subsequent ordinary `lib/main.dart` builds both passed, exit 0. Startup registration recovery is separate from UI acceptance.
- Android acceptance: run1 exited 0 with 26 observations and 20 screenshots on Android API 36 / `xunqiu64_api36`, density 420, at 1080x2400/1.0 and 840x1100/2.0. Physical scrolling and taps verified direct entry, cross-tab entry before header collapse, each source-status filter/count and matching row, original no-source question identity, retained selection after Android Back, all-filter restoration and zero-match recovery. Every observation has zero framework errors, blocked database accesses and executor calls, with four list providers each read once; the filtered app-PID log is empty. All 20 screenshots were visually inspected. Off-screen scroll content and existing subtitle ellipsis are unchanged; synthetic pending-list isolation is not persisted metric-consistency or status-write acceptance.
- Restoration: ordinary APK SHA-256 is `b80626db2a5c7b766b5c18ea45e3f90bae76d33a94291ea2308ec74f4be574f0`; its installed hash matches, its kernel has no DL fixture extension/entrypoint markers, and both source/test hashes match the accepted fixture. The ordinary app was not launched. Database SHA-256 before acceptance, before restoration and after installation remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display returned to 1080x2400/density 420/font scale 1.0; no forward or app PID remained. The emulator is stopped, ADB lists no devices and no matching task runtime remains; shared services were retained.
- Evidence/cleanup: `anchor-dl026-20260913-evidence.json` in this task's evidence directory, SHA-256 `3d21054ddaf7ccb67980f6e4ce335f056d48f1657c1e7fb6cde6ade3ad0d8be7`, records 38 retained artifact hashes, source/APK hashes, baseline/startup classifications, visual review, restoration, shutdown and cleanup. Exactly four newly created temporary files with prefix `anchor-dl026-20260913-01a09531-` were hash-recorded and removed: fixture source, device controller and emulator output/error logs. Original acceptance logs/JSON and 20 screenshots remain evidence. Both protected reports retain their baseline hashes; they, `%SystemDrive%`, previous-task helpers/backups, historical worktrees and caches remain untouched.

### Completed Leaf DL-025 - Short-screen library read-error recovery

- Baseline: `4f397304af0f62b0a8f042e8d5dfb8d9b2f0bd59`, clean tracked files. Both protected reports retain their baseline hashes; they and the unowned `%SystemDrive%` directory remain untracked. No task runtime or ADB device is active.
- Goal/output: failed source, knowledge-point, question and pending-list reads show reachable error, retry and diagnostic controls at ordinary and short/200% layouts. Retrying reloads only the selected list, retains its existing in-flight refresh presentation and recovers to the existing empty content without a framework overflow.
- Inputs: the four direct list-error branches in `knowledge_base_screen.dart`, the existing state viewport, the unchanged shared `KnowledgeLibraryErrorState` and `knowledge_library_layout_widget_test.dart`. Search slivers and detail-page embedded errors are different consumers and must retain their existing layout contracts.
- Owned files: `lib/features/knowledge_base/knowledge_base_screen.dart`, `test/knowledge_library_layout_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary helpers use `anchor-dl025-20260913-01a09531-` in the system temporary directory; retained evidence uses `anchor-dl025-20260913-` under this task's existing visualization directory.
- Acceptance: reproduce on unchanged production code using controlled failed reads and delayed retries for all four tabs at 390x844/1.0 and 320x420/2.0. Verify full retry/diagnostic reachability, pending-refresh presentation, successful recovery and unchanged unrelated-provider read counts. Run related Agent/library/search tests, full Flutter tests, analysis, format and diff checks. Verify physical Android scrolling, diagnostic-copy feedback and retry/recovery on both emulator configurations with synthetic providers and blocked database access; inspect screenshots, restore ordinary APK/display/database, stop the emulator and remove only this leaf's temporary helpers.
- Boundary: no shared error-widget redesign, string/redaction/feedback changes, provider or repository protocol, nonempty-list filtering, bulk verification, model calls, persisted data, release or automation changes. Reuse the existing scrolling state container for the four confirmed error branches; rename that private helper only if needed to reflect its broader state content.
- Status: `completed`; local commit: `39fcc57` (`fix(library): keep read-error recovery reachable (DL-025)`). The contract preceded test and production edits. Fresh local and Android acceptance, ordinary-APK restoration and temporary cleanup are complete; DL-024 results remain historical evidence.
- Reproduction: the unchanged screen passed all four ordinary read-error/retry cases and failed all four short/200% cases with a 77-pixel bottom overflow (`anchor-dl025-20260913-baseline2.log`). The first baseline included unnecessary forced scrolling of already visible ordinary controls and an incorrect test assumption that Riverpod refresh hides the prior error; those test-only expectations were corrected before the valid baseline. The existing refresh presentation is preserved.
- Implementation: the four list-error branches now reuse the existing coordinated state viewport. Its private name is generalized to `_LibraryStateViewport`; empty-state and search-sliver behavior is otherwise unchanged. Shared error strings, diagnostic/feedback callbacks, provider invalidation and repository behavior are untouched. Local and Android gates pass.
- Local checks: all 74 Agent-home/library/search checks and 613 full Flutter tests passed, exit 0, including the eight new error/retry cases. Analysis found no issues (61.1 seconds); both changed Dart files format with zero changes and `git diff --check` passes. Logs use `anchor-dl025-20260913-` in the current evidence directory. The synthetic fixture APK built successfully; its kernel contains this leaf's extension and temporary entrypoint, and source/helper/APK hashes are recorded in `anchor-dl025-20260913-fixture-inspection.json`.
- Device continuation: the previous startup command was no longer available, no emulator process or ADB device remained, and no completed startup record existed. A new cold boot completed but reported the Anchor package as unknown. Building/installing the current ordinary entrypoint without launching it restored package visibility; its installed hash matched and the database hash remained the recorded baseline before fixture installation. The first APK-path guard omitted Android's `~` path character and stopped before fixture installation; correcting that helper-only guard required no production change. Startup-check evidence and temporary-file hashes are retained; startup attempts are not Android acceptance.
- Android acceptance: device run1 exited 0 with 48 observations and 20 screenshots on Android API 36 / `xunqiu64_api36`, density 420, at 1080x2400/1.0 and 840x1100/2.0. All four lists passed synthetic error-message reachability, physical scrolling to each action, native diagnostic-copy feedback, delayed retry with the previous error retained, selected-list-only reload and successful empty-state recovery. Every observation has zero framework errors, blocked database accesses and executor calls; the filtered app-PID log is empty. All 20 screenshots were visually inspected. Feedback was not submitted. Individual control bounds were collected after scrolling to each target; off-screen header/tab content is not a new layout failure.
- Restoration: ordinary `lib/main.dart` APK SHA-256 is `78371bc87da54de7ce23930169d59d351696be0ff43a88cdee758634a2a7665d`; the installed hash matches, its kernel has no fixture markers, and both source/test hashes match the accepted fixture. The ordinary app was not launched. Database SHA-256 before acceptance, before restoration and after installation remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display returned to 1080x2400/density 420/font scale 1.0; no forward or app PID remained. The emulator is stopped, ADB lists no devices and no matching task runtime remains.
- Evidence/cleanup: `anchor-dl025-20260913-evidence.json` in this task's evidence directory, SHA-256 `13a19be9b181c389a6c0515afba96ea02838268261a8496150646072b3306548`, records 42 retained artifact hashes, source/APK hashes, attempt classifications, visual review, restoration and cleanup. Exactly seven newly created temporary files with prefix `anchor-dl025-20260913-01a09531-` were hash-recorded and removed: fixture source/controller, four emulator output/error logs and the temporary fixture APK copy. All original acceptance logs/JSON and 20 screenshots remain evidence. Both protected reports retain their baseline hashes; they, `%SystemDrive%`, previous-task helpers/backups, historical worktrees and caches remain untouched.

### Completed Leaf DL-024 - Agent home pre-navigation read recovery

- Baseline: `391cd818915f863289f67a6f3f776c65c34eeacf`, clean tracked files. Both protected reports and the unowned `%SystemDrive%` directory remain untracked and untouched. No Flutter/Dart/emulator process was running at entry.
- Goal/output: checkpoint and recommended-knowledge-point reads started by an Agent-home tap show a visible error on failure and allow retry through the original action. A failure or result arriving after the home route or provider scope is disposed must not navigate, save a checkpoint, refresh providers or show a snackbar. Successful checkpoint resume, knowledge-point navigation and missing-record fallback retain their existing behavior.
- Inputs: `AgentHomeScreen._startPlan`, `_openFocusPoint`, `_showAgentMessage`, `_LearningAgentPlanCard` callbacks and `test/agent_home_navigation_widget_test.dart`. Provider-list errors already have recovery UI; the two direct reads do not have a catch boundary.
- Owned files: `lib/features/agent/agent_home_screen.dart`, `test/agent_home_navigation_widget_test.dart`, `lib/features/knowledge_base/knowledge_base_screen.dart`, `lib/features/knowledge_base/knowledge_answer_evidence_quality_badges.dart`, new `test/knowledge_library_layout_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. The library files and badge were added to this contract after reproduction and before their regression or production edits. Temporary Android helpers use `anchor-dl024-20260912-01a09011-`; retained evidence uses `anchor-dl024-20260912-` in the current task's visualization directory.
- Acceptance: add controlled read failures/futures against the unchanged production screen; cover mounted retry, late success/failure after disposal and missing-record behavior for both entry points. Run focused Agent navigation/session tests, full Flutter tests, analysis, formatting and diff checks. Verify actual Android error/retry/back interaction at normal and short-large-text configurations with synthetic in-memory stores and blocked database access; retain screenshots, restore the ordinary APK/display/database baseline, stop the emulator and remove only newly created temporary helpers.
- Boundary: no planner priority, checkpoint schema/revision/conflict policy, goal persistence, runtime resume sequencing, navigation concurrency redesign, repository/provider protocol, unrelated layout redesign, live model, physical device, release or automation changes. The narrowly amended library layout scope below is required to finish the missing-point fallback acceptance.
- Status: `completed`; local commit: `414e846` (`fix(agent): recover home reads and library fallback (DL-024)`). Contracts were recorded before regression or production edits. Baseline home-navigation tests passed 20 and failed four, exit 1: mounted checkpoint/knowledge-point lookup errors had no retryable message, and late failures after scope disposal escaped error handling. The other four new checks and all 16 original checks already passed. Baseline log: `anchor-dl024-20260912-baseline.log` in the source task's visualization directory.
- Implementation: each direct read now has a narrow catch boundary and mounted checks before provider access, error UI and post-read continuation. Mounted failures use the existing snackbar helper and leave the original action available; downstream checkpoint resume, navigation and missing-record recovery are unchanged.
- Pre-layout local checks: 24 home-navigation checks, 61 related Agent checks and 585 full Flutter tests passed, all exit 0. Analysis found no issues (34.6 seconds), two Dart files have unchanged formatting and `git diff --check` passed. These checks precede the library expansion and are not final acceptance of the current diff. Logs use `anchor-dl024-20260912-` in the current task's visualization directory. Android acceptance and emulator/ordinary-APK restoration remain pending.
- Intermediate Android fixture failure: run1 stopped at its first observation, with no screenshots or database access, after the diagnostic tree walk registered route inherited-widget dependencies on unrelated elements. The temporary helper now observes navigation without `ModalRoute.of` tree queries; production code is unchanged. The failed run and app-PID framework log are retained, not counted as acceptance. A corrected run remains pending.
- Android run2 completed the normal-size flows (16 clean observations, six inspected screenshots), then stopped with a 30-pixel bottom overflow after switching from the knowledge-library fallback to short-large text; the first short checkpoint tap did not reach its read. This is not a passing short-screen gate. The fixture now records full error diagnostics and verifies hit-test visibility; configuration changes start from the neutral fixture route. Production code and completed local tests are unchanged while the overflow is being localized.
- Android run3 repeated the normal flows cleanly, then stopped because its physical scroll loop did not wait for kinetic scrolling to settle and could not hit the short-screen checkpoint button. A bounded short-screen probe with slower gestures reached the original button, displayed the expected read error, and recorded no framework/database errors. The probe screenshot/JSON and all failed runs remain retained; the controller now uses slower gestures and a settling delay. The full short-screen gate is still pending.
- Android run4: 31 observations passed, including normal/short error, retry, back and late-read checks. The final short missing-point fallback reproduced an 83-pixel overflow in the unchanged library `_EmptyState` Column at line 3912; its diagnostic screenshot and full error details are retained. This is a pre-existing destination-layout defect, not a read-catch regression, and the run remains failed (exit 1).
- Contract amendment: keep the same recovery leaf active and make the library fallback usable when its fixed metric header consumes the short/large-text viewport. Permit coordinated vertical scrolling of that header and bounded empty-state contents, while preserving the current tabs, metrics, empty messages, source-import action and navigation semantics. Add ordinary/short large-text coverage for all empty library tabs plus tab reachability, reproduce on the unchanged library file first, then repeat related Agent/library tests, full Flutter tests, analysis and both Android configurations. No model/search/repository behavior changes are authorized by this amendment.
- Library baseline: the unchanged library file passed all four ordinary empty-tab checks and failed all five short/large-text checks, exit 1 (`anchor-dl024-20260912-layout-baseline.log`). The source tab overflowed by 129 pixels; knowledge-point, question and pending tabs overflowed by 53 pixels in the widget fixture. The correction makes the metric header vertically scrollable with tabs above the nested content and gives bounded empty states a scrollable viewport; unbounded embedded empty states retain their existing centered layout. Verification is pending.
- Layout iteration: the first correction passed the nine new layout checks but failed two existing search/citation checks (29 passed, two failed overall) because an overlapping pinned sliver covered the search clear control after scrolling. Tabs now stay in the nested body above the tab contents instead of painting over them. Search tests are unchanged and must pass before this correction is accepted.
- Second layout iteration: all nine layout checks and the ordinary search check pass; the related run has 30 passes and one failure, exit 1 (`anchor-dl024-20260912-layout-focused2.log`). Full diagnostics identify a 47-pixel horizontal overflow in `_EvidenceQualityBadge` at `knowledge_answer_evidence_quality_badges.dart:129`: its unconstrained label cannot fit the history row's 124-pixel inner width at 200% text. The existing narrow search/answer/clear regression remains unchanged.
- Badge contract amendment: constrain the existing badge label so it can wrap within the available row width; preserve the labels, quality decisions, icons, colors and tooltip messages. Add ordinary/200% regression cases for the long labels in the owned layout test, reproduce before changing the badge, then rerun library/search, Agent, full Flutter, analysis and Android gates. No evidence-quality or history persistence behavior change is included.
- Badge baseline: all three ordinary long-label checks passed and all three 200% checks reproduced the same 47-pixel right overflow on the unchanged badge, exit 1 (`anchor-dl024-20260912-badge-baseline.log`). The label now uses a loose flexible child to wrap without truncation; final verification remains pending.
- Corrected library/search gate: 37 checks passed, exit 0 (`anchor-dl024-20260912-layout-focused3.log`), including all 15 new empty-state/tab/badge checks and both unchanged search-answer-citation-clear cases. Formatting five changed Dart files made zero changes. Related Agent, full Flutter, analysis and rebuilt Android-fixture acceptance are now being rerun for the complete diff.
- Continuation verification: the original command `call_rfXVa8f0gsBxkCiTT97usIfs` completed with exit 0 after all final gates. Its retained `final-focused.log`, `final-full.log`, `final-analysis.log` and `final-format-diff.log` confirm 98 focused checks, 600 full Flutter tests, no analysis issues (35.7 seconds), five formatted Dart files with zero changes and a passing diff check. Subsequent original-task edits touched only the temporary device fixture/controller; these passing application gates are retained without repetition. The current task verified all five application/test hashes, both protected-report hashes and the previous ordinary APK hash. No Flutter/Dart/emulator process or ADB device remained at entry.
- Recovery plan: rebuild the updated synthetic fixture, verify error/retry/back and all amended library empty-tab/history-badge cases at both Android configurations, then restore the ordinary APK/display/database baseline and stop the emulator. New helper copies use `anchor-dl024-20260912-01a09531-` in the system temporary directory; new retained evidence goes under `D:\Agent\codex\visualizations\2026\09\12\01a09531-76ee-7b91-839d-0daa7fe4c68e`. Original logs, helpers and prior failed runs remain retained.
- Recovery device findings: the first resumed run exited 1 before any observation because the controller raced service-extension registration; a bounded readiness wait corrected only the controller. Run2 passed 18 normal-size observations, then recorded `ScrollPosition.didOverscrollBy` / `activity!.isScrolling` assertions while physically scrolling after a library tab change. Database access and executor counts stayed zero. This is an unresolved failure of the amended nested-scroll layout, not a passing device gate. The first fixture build also waited on Flutter's automatic version-check fetch; cancelling that fetch allowed its build to finish with exit 0, and later builds use the per-command `--no-version-check` flag.
- Scroll contract amendment: reproduce coordinated dragging during and after empty-tab changes with frame-by-frame pointer movement in `test/knowledge_library_layout_widget_test.dart`; correct the scroll activity lifecycle inside the already owned library screen while preserving header/tab reachability, horizontal tab navigation and complete empty-state controls. Rerun the affected checks, full suite, analysis and device acceptance after the correction. The earlier 98/600 pass remains dated pre-correction evidence. The one bounded read-only auxiliary audit returned no result and was stopped; the primary agent owns the investigation and verification.
- Scroll reproduction/correction: after aligning the test's first animation frame with device timing, `scroll-baseline2.log` reproduced the same gesture assertion. A temporary diagnostic confirmed that a non-adjacent tab transition attached an idle neighboring position during an active drag. `AlwaysScrollableScrollPhysics` alone did not resolve the ordinary-size regression and is not retained. Each tab now inherits the nested primary scroll controller only while selected; neighboring pages use their own scrolling scope. Both frame-by-frame transition regressions pass, and their final form also checks horizontal swipe navigation. Diagnostic prints were removed before the final gates; application/test changes now require refreshed local and Android acceptance.
- Final local gates after scroll correction: 100 focused Agent/library checks and 602 full Flutter tests passed, exit 0. Analysis found no issues (67.6 seconds). Formatting verified all five changed Dart files with zero changes after the initial formatting pass; `git diff --check` passed. The final logs use `anchor-dl024-20260912-final-` in the current task's visualization directory. The updated fixture build/run3 now repeats both Android configurations; device acceptance, ordinary-APK restoration and the scoped local commit remain pending.
- Resumed device run3 exited 1 after 44 clean observations and 22 screenshots: all recorded framework-error, blocked-database and executor counts are zero, but its large scroll steps could not locate the short-screen partial-quality badge. The completed normal-size screenshots were visually inspected; this incomplete run is retained and is not complete Android acceptance.
- Resumed device run4 used smaller badge-location gestures and reached the partial badge, but first entering search directly from the short-screen missing-point fallback, before collapsing the metric header, produced a 79-pixel bottom overflow in `_KnowledgeSearchTabState.build` (Column at line 325, available height 16). The fixture recorded the error even though the filtered log was empty. Both runs restored display settings and forwards; the synthetic fixture remains installed pending correction and final restoration.
- Search-layout contract amendment: reproduce initial search and direct knowledge-point-to-search entry with the expanded metric header at short size and 200% text. Make the search controls and contents reachable without a fixed-height control overflow; preserve query identity/debounce, answer generation/saving/retry, history navigation and horizontal tabs. Changes remain in the owned library screen and layout test. Refresh relevant search/layout and complete local gates, then repeat the amended Android paths; do not hide the first-frame overflow by pre-scrolling in the controller.
- Search reproduction/correction: both expanded-header entry regressions fail on the unchanged search layout with a 49-pixel widget overflow (`search-layout-baseline2.log`); the first baseline also had a test-only off-screen horizontal tab tap, corrected before the valid reproduction. Search controls, results and history now use slivers in one coordinated viewport. The answer panel keeps its own bounded scrolling without inheriting that viewport's primary controller. The existing 22 search checks remain unchanged and pass with the 19 layout checks (41 total, exit 0). A further short-screen query-error/retry/clear check passes after its locator scrolls to the lazily built retry control; its earlier locator-only failures are retained. Query, answer and persistence logic are unchanged. Final local and Android gates remain pending.
- Refreshed final local gates: 103 focused Agent/library checks and 605 full Flutter tests passed, exit 0. Analysis found no issues (13.2 seconds); all five changed Dart files have unchanged formatting and `git diff --check` passes. Logs use `anchor-dl024-20260912-search-final-`. The database hash is still the recorded baseline. A new fixture build is in progress; its controller now checks direct search entry before any header-collapsing scroll at both Android sizes and then repeats all existing error/retry/back, late-read, empty-tab and badge cases. Final Android acceptance, ordinary APK restoration, cleanup and local commit remain outstanding.
- Final Android acceptance: resumed run5 exited 0 with 50 observations and 26 screenshots on Android API 36 / `xunqiu64_api36`, density 420, at 1080x2400/1.0 and 840x1100/2.0. Error/retry/back, pending-read exit, late success/failure, missing-record fallbacks, direct search before header scrolling, four empty tabs, source-import reachability and legacy/partial/refused labels passed. Every observation has zero framework errors, blocked database accesses and executor calls; the filtered app-PID log is empty. All 26 screenshots were visually inspected. Existing title/subtitle ellipsis and content outside the scrolling viewport are not claimed as changed. Earlier failed runs remain retained and are not counted as complete acceptance.
- Final restoration: ordinary `lib/main.dart` debug APK SHA-256 is `2f8152dafd2af69641346e85f28800196042870f659da02fae84bbd9446495e6`; its installed hash matches, all fixture markers are absent, and all five source/test hashes match the accepted fixture. The ordinary app was not launched. Database SHA-256 before and after restoration remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display returned to 1080x2400/density 420/font scale 1.0, no forward or app PID remained, the emulator was stopped, ADB lists no devices and no matching task runtime remains.
- Final evidence/cleanup: `D:\Agent\codex\visualizations\2026\09\12\01a09531-76ee-7b91-839d-0daa7fe4c68e\anchor-dl024-20260912-evidence.json`, SHA-256 `bd2cffa326c7626371d9917db70755653961104b416c80454fc8b84c596d99be`, records source, log and screenshot hashes, failed-attempt classifications, restoration and cleanup. Exactly 17 temporary files created by this task were hash-recorded and removed: its fixture/controller, two emulator logs and 13 contact sheets with prefix `anchor-dl024-20260912-01a09531-`. Original screenshots and logs remain evidence. The two protected reports retain their baseline hashes and remain untracked; prior-task helpers/backups, `%SystemDrive%`, historical worktrees and caches are retained. All acceptance, restoration, cleanup and scoped local-commit steps are complete.

### Completed Leaf DL-023 - Agent follow-up detail read failures and route-exit recovery

- Baseline: `44ff7cf`, clean tracked files. Both protected reports remain untracked. `D:\workspace4Cursor\learn\anchor\%SystemDrive%` appeared during runtime probes and contains four Windows cache files; ownership has not been established, so it is retained and excluded from staging. No Flutter/Dart/emulator process was running at leaf entry.
- Goal/output: a failed before/after follow-up record read gives a visible retryable error; leaving the detail route before an awaited stage finishes prevents new navigation, record writes, provider refresh and snackbar work. An insert already started may finish for its original record without using the disposed page. One follow-up action at a time and mounted handled/unhandled/save-failure behavior remain unchanged.
- Inputs: `AgentSessionDetailScreen._runFollowUpAction`, `_completedSessionCount`, `_recordFollowUpHandled`, `_FollowUpActionCardState._runAction` and `test/agent_session_detail_widget_test.dart`. Baseline tests cover immediate success/failure, but not pending session reads, scope disposal or a pending handled-record insert.
- Owned files: `lib/features/agent/agent_session_detail_screen.dart`, `test/agent_session_detail_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary Android helpers use `anchor-dl023-20260912-01a09011-`; retained evidence uses `anchor-dl023-20260912-` in this task's visualization directory, outside the repository.
- Acceptance: reproduce failures against the unchanged detail screen with controlled read/insert futures; cover mounted read retry, pending-action deduplication, late before/after reads, insert completion/failure after exit and scope disposal. Run focused detail/Agent tests, full Flutter tests, analysis, formatting and diff checks. Verify pending/error/retry/back controls in the Android emulator at ordinary and short-large-text sizes with synthetic in-memory records and blocked database access; preserve screenshots, restore the ordinary APK/display/database baseline and stop the emulator.
- Boundary: no learning-session schema, completion matching, follow-up semantics, Tutor/Interview model task, persistence transaction, source evidence, provider protocol, release or automation changes.
- Status: `completed`; local commit: `897dda2` (`fix(agent): guard follow-up detail lifetime`). The contract was recorded before adding regressions or changing production code. The unchanged screen passed 14 checks and failed four: mounted initial/final read failures, a late initial success after route exit and a late initial failure after scope disposal. Duplicate-action ownership and the final-read/insert exit checks already passed.
- Implementation: the before/action/after/save sequence shares error handling and checks route lifetime after each await. Mounted read errors remain visible and retryable; insert errors retain their existing save-failure message. Post-insert refresh now runs in the mounted caller, not the helper. Completion matching, persistence schema and action labels are unchanged.
- Local checks: 37 focused Agent checks and 577 full Flutter tests passed, exit 0; analysis found no issues (73.5 seconds). Two changed Dart files have unchanged formatting and `git diff --check` passes. Logs use `anchor-dl023-20260912-` in this task's visualization directory; unchanged suites are not repeated for fixture-only corrections.
- Device acceptance: Android API 36 on `xunqiu64_api36`, density 420, at 1080x2400/1.0 and short 840x1100/2.0. Run2 exited 0 with 28 observations and 10 screenshots: pending reads disabled both actions; repeated taps did not overlap; mounted before/after read errors allowed retry; completing a synthetic Tutor session wrote one handled record; late reads after exit started no write; already-started inserts settled safely after exit. Every observation has zero framework errors and blocked database accesses, and the filtered app-PID log is empty. All ten final screenshots were inspected; retry was exercised after the transient snackbar expired. The fixed synthetic memory index does not establish persisted follow-up status refresh, and the existing narrow app-bar ellipsis is not claimed as changed.
- Intermediate fixture failure: run1 stopped at the database-protection assertion because the actual Tutor screen's knowledge-point list lacked a synthetic provider override; the blocked factory prevented database access. Its six observations, two screenshots and failure record remain retained, not counted as complete acceptance. Adding the missing list override changed only the temporary helper; no production correction or repeat full suite was required.
- Restoration: a cold-boot package lookup reported unknown package; reinstalling the verified previous ordinary APK without launching it restored visibility and confirmed the database baseline. After acceptance, the rebuilt ordinary `lib/main.dart` APK SHA-256 is `410a61a33371810c3fdacfe6afeed991c1d5e5a72ec364ad51e795ddb35e43dd`; its installed hash matches and the fixture extension/entrypoint are absent from its kernel. The ordinary app was not launched. Database SHA-256 before fixture, before restoration and after installation remains `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display is 1080x2400/density 420/font scale 1.0, no forward remains, and the emulator is stopped; ADB lists no devices and matching runtimes are gone.
- Evidence/cleanup: the evidence JSON above has SHA-256 `9d226ae71ae6c6788b27a12f4f6e616181ead7b00d235957c0fdf7e7a7e334e9` and records code/log/screenshot hashes and restoration. Removed exactly five newly created temporary files with prefix `anchor-dl023-20260912-01a09011-`: fixture, controller, previous ordinary APK and emulator output/error logs, after recording and checking their hashes. Both protected reports keep their baseline hashes and remain untracked. Pre-existing DL-022 helpers, the unowned `%SystemDrive%` directory, historical worktrees and caches remain untouched.

### Completed Leaf DL-022 - Agent session launch lifecycle ownership and route-exit recovery

- Baseline: `fc62d64`, clean tracked files with only the two protected user reports untracked. No Flutter, Dart, emulator or fixture process is running.
- Goal: make Agent session start, tool checkpoint callbacks, final execution checkpoint and completion reflection save own their in-flight operations and stop post-await UI/provider/database work after the route exits. A mounted route must retain existing checkpoint retry/conflict, executor result, cancellation and completion-save retry behavior; runtime checkpoint schema, executor ordering and session persistence semantics remain unchanged.
- Inputs: `_startSession` in `lib/features/agent/agent_session_launch_screen.dart` has no entry guard before its initial state update; the executor checkpoint callback can begin `persistCheckpoint` without a mounted check, and the final checkpoint starts immediately after executor await without a route boundary. `_saveAgentSessionRecord` starts reflection persistence and then inserts the learning session without checking mounted between awaits. Existing Agent launch coverage has delayed executor and checkpoint failure checks but no duplicate-start or late-checkpoint/reflection route-exit checks.
- Output: start, completion-save and checkpoint retry share exclusive ownership of the mounted session; repeated or conflicting actions while persistence is pending are ignored; route exit before checkpoint/executor/reflection/session completion causes no new persistence, provider invalidation, snackbar or navigation work. Mounted failures remain visible and retryable.
- Owned files: `lib/features/agent/agent_session_launch_screen.dart`, new lifecycle coverage in `test/agent_session_launch_lifecycle_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Any device fixture material will use the `anchor-dl022-20260910-01a081db-` prefix outside the repository.
- Acceptance: first reproduce duplicate pending start and late callback/final checkpoint/reflection completion on the unchanged screen; add focused widget coverage for start/completion ownership, mounted route exit and mounted retryable failure. Run focused Agent/knowledge/session checks, full Flutter tests, analysis, formatting and `git diff --check`. If Android verification is needed, use delayed synthetic checkpoint/executor/session services with database access blocked at normal and short large-text configurations, retain screenshot/log evidence, then restore the ordinary APK/display/database baseline and remove only this leaf's temporary helper.
- Boundary: no Agent planning schema, runtime checkpoint schema, executor tool ordering, conflict resolution policy, session summary format, model provider protocol, release operation, credential handling or automatic wake-up change.
- Pre-fix reproduction: the exact baseline production file failed four of the initial five lifecycle checks: duplicate start wrote two checkpoints instead of one; callbacks and executor results wrote after route exit; late reflection checkpoint completion inserted a session after route exit. Completion deduplication already passed. The fixed production file has been restored.
- Status: `completed`; local commit: `44ff7cf` (`fix(agent): guard session launch lifetime`). Start, completion-save and checkpoint retry now share an in-flight guard, and the tool callback aborts when its route exits. Mounted checkpoint, execution, cancellation and reflection retry behavior is preserved.
- Local checks: 9 focused lifecycle checks, 73 related checks and 569 full Flutter checks passed on 2026-09-11; analysis found no issues. Original exit records confirm the related/full/analysis results. This task reran all 9 focused checks (exit 0), verified two Dart files are formatted without changes and passed `git diff --check`; unchanged full/analysis gates were not repeated. The earlier focused scroll-location failure was corrected before the passing suites.
- Device acceptance: Android API 36 on `xunqiu64_api36`, density 420, at 1080x2400/1.0 and short 840x1100/2.0. The final controller exited 0 with 20 observations and 14 screenshots: duplicate start/completion taps did not repeat writes; restart was disabled while reflection saved; leaving before a pending checkpoint completed prevented later execution/session insertion; mounted checkpoint failure recovered through retry. Every observation has zero framework errors and blocked database accesses. The first fixture startup failed because it replaced the global sqflite factory; the corrected fixture injects a blocked test helper without changing production services. That first run is retained and not counted as a pass.
- Restoration: this task rebuilt and installed the ordinary `lib/main.dart` debug APK, SHA-256 `fc420649f4b5856ebeb811598a3bc39ba886072688e42d037ed740c668bdf636`; the installed hash matches and neither the DL-022 service extension nor temporary entrypoint occurs in its kernel. The ordinary app was not launched. Database SHA-256 before the original fixture, before restoration and after installation is `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`. Display is 1080x2400/density 420/font scale 1.0, no port forward remains, and the original task's emulator has been shut down.
- Evidence/retention: original run logs, JSON and screenshots remain under the previous task's visualization directory; this task records their hashes, original command locations and restoration in its evidence JSON above. Five pre-existing DL-022 helper/backup/log files in the system temporary directory are retained. Both protected reports retain their baseline hashes and remain untracked. No new temporary helper was needed. A read-only history-search subagent returned no findings within ten minutes and was stopped; all acceptance was verified directly by the primary agent.

### Completed Leaf DL-021 - Tutor session lifecycle ownership and route-exit recovery

- Baseline: `b91ec05`, clean tracked files with only the two protected user reports untracked.
- Goal/output: one explanation-generation and one answer-submission operation per mounted screen; repeated actions during key/evidence/AI work were ignored; route exit before key/evidence/AI/closure/summary completion performed no UI/provider or new persistence continuation; mounted generation failure remained visible and retryable. Evidence selection, tutor turn schema, review closure and session-summary semantics remained unchanged.
- Pre-fix reproduction: the unchanged screen accepted two pending explanation key calls and let the late key result call `setState()` after the route was removed.
- Implementation: `_explain` now claims `_isGenerating` before key lookup, checks route lifetime through evidence/provider/AI/session awaits and uses a route-exit sentinel to stop helper chains. `_submitAnswer` checks mounted before AI feedback, review closure and session-summary persistence and before invalidating providers.
- Local checks: 6 focused Tutor lifecycle checks, 44 related Tutor/Agent/knowledge checks and 560 full `flutter test --no-pub` checks passed. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two Dart files with zero changes; `git diff --check` passed.
- Device/evidence boundary: no Android fixture or emulator process was created for this leaf; controlled widget fixtures covered key, evidence, AI, closure and summary route-exit behavior. Evidence: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl021-20260910-evidence.json` (SHA-256 `91d4e99fc83175917c1c4d43a267364ced798c60521a8da5b89d9b5941286f3a`). No API key or private endpoint was written to source, evidence, device or logs.
- Status: `completed`; local commit: `fc62d64` (`fix(agent): guard tutor session lifetime`).

### Completed Leaf DL-020 - Interview session lifecycle ownership and route-exit recovery

- Baseline: `90f63d2`, clean tracked files with only the two protected user reports untracked.
- Goal/output: one startup/evaluation/next-question/completion operation per mounted screen; repeated actions while matching operations were pending were ignored; route exit before key/model/persistence completion performed no UI/provider/database continuation; mounted evaluation failure remained visible and retryable. Interview flow ordering, turn schema, mastery semantics and closure persistence semantics remained unchanged.
- Pre-fix reproduction: the unchanged screen accepted duplicate pending answer-evaluation actions, allowed a late startup key result to continue into repository work after route exit, and had no complete mounted boundary before completion invalidation.
- Implementation: `_startSession` now owns startup before key lookup and checks route lifetime through evidence/provider/model/session awaits; `_evaluateCurrentAnswer` owns evaluation and checks mounted before closure, mastery and invalidation; `_nextQuestion`, `_finishSession` and confirmed exit own their operations and stop late UI/provider work. The question evidence tile receives a local `Material` ancestor to satisfy Flutter's ListTile background contract without changing its presentation.
- Local checks: 7 focused interview lifecycle checks passed. Full `flutter test --no-pub`: 554 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two Dart files with zero changes; `git diff --check` passed.
- Device/evidence boundary: no Android fixture or emulator process was created for this leaf; controlled widget fixtures covered startup, evaluation, next-question and completion route-exit behavior. Evidence: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl020-20260910-evidence.json` (SHA-256 `0924032eb596e405196055c585f99565c4719e809c002b8bae5e9f04e4ed6602`). No API key or private endpoint was written to source, evidence, device or logs.
- Status: `completed`; local commit: `ba48f5b` (`fix(agent): guard interview session lifetime`).

### Completed Leaf DL-019 - Programming exercise generation and answer submission ownership

- Baseline: `61ca806`, clean tracked files with only the two protected user reports untracked.
- Goal/output: one generation request and one answer-evaluation request per mounted screen; repeated taps during API-key lookup or AI work are ignored; route exit before key/task/context completion performs no UI/provider or new persistence work; mounted failures remain visible and retryable. Exercise schemas, citation validation, persistence ordering and mastery semantics remained unchanged.
- Pre-fix reproduction: the unchanged screen accepted duplicate generation/submission taps while API-key lookup was pending and had no mounted boundary before late key/task results continued into UI or provider work.
- Implementation: `_generateExercises` and `_submitAnswer` now acquire busy ownership before API-key lookup, check `mounted` after key/task/context awaits and before each persistence stage, and invalidate providers only after mounted UI state is updated.
- Local checks: 6 focused programming-exercise lifecycle checks passed. Full `flutter test --no-pub`: 547 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two Dart files with zero changes; `git diff --check` passed.
- Device acceptance: Android API 36 on `xunqiu64_api36`, density 420, normal 1080x2400/1.0 and short 840x1100/2.0. Delayed synthetic key/generation services showed disabled loading controls, duplicate-entry blocking, route exit before delayed results, mounted retryable failure and a short-screen scroll path to the retry button. Filtered logcat recorded zero fatal exceptions, Flutter errors, RenderFlex overflows, unhandled Dart exceptions or blocked database accesses.
- Restoration/evidence: ordinary `lib/main.dart` debug APK SHA-256 `19b97844607efd1738c4b8e5e6b76c9a055f6d62d85d25375c15f6e60822b08e`; installed hash matched and no fixture marker entries remained. The database hash was `7fdb71e9859fe1241236e89a08004ade48e2d55663763903a66b974f6faf4c20` before and after ordinary APK installation; ordinary app startup later produced `7e23b558a9d32ad0c8bb859b156b1043728db435f21c913deccca29eae228287`, recorded separately. Display returned to 1080x2400/density 420/font scale 1.0, the emulator was stopped and ADB has no devices. Evidence: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl019-20260910-evidence.json` (SHA-256 `a918d25919c22b26ce72c024028e9b250f200763d04acda88cef47dcd35d2d43`). Temporary files created for this leaf were removed after evidence capture; protected user reports remain untracked and unchanged.
- Status: `completed`; local commit: `90f63d2` (`fix(agent): guard programming exercise lifetime`).

### Completed Leaf DL-018 - Concept learning path generation ownership and route-exit recovery

- Baseline: `9d82a32`, clean tracked files with only the two protected user reports untracked.
- Goal/output: one candidate-generation request per mounted screen; repeated taps during API-key lookup or prerequisite analysis are ignored; route exit before key/task completion performs no UI/provider work; mounted failures remain visible and retryable. Point selection, relation sanitization, save semantics and path rendering remained unchanged.
- Pre-fix reproduction: the unchanged screen allowed two pending API-key calls from two taps and let a successful late key result call `setState` after the route had been removed.
- Implementation: `_generateCandidates` now acquires `_isGenerating` before the API-key await, blocks generation while saving, checks `mounted` after the key and prerequisite task awaits and retains the existing missing-key/error messages.
- Local checks: 5 focused concept-path lifecycle checks and 32 related knowledge/agent checks passed. Full `flutter test --no-pub`: 542 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two Dart files with zero changes; `git diff --check` passed.
- Device acceptance: Android API 36 on `xunqiu64_api36`, density 420, normal 1080x2400/1.0 and short 840x1100/2.0. Delayed synthetic key/task services showed disabled loading controls, duplicate-entry blocking, route exit before delayed results and restored controls after completion. Filtered logcat recorded zero fatal exceptions, Flutter errors, RenderFlex overflows, unhandled Dart exceptions or blocked database accesses.
- Restoration/evidence: ordinary `lib/main.dart` debug APK SHA-256 `b01b2b781dc6ff14939e62d090f4359437792991685ab12276752aa601972728`; installed hash matched and no fixture marker entries remained. The database hash was `cf52c44b7a66b228dd84ff8e997628ff0d1114246f58da1a68e2a0e202e82c26` before and after ordinary APK installation; ordinary app startup later produced `7fdb71e9859fe1241236e89a08004ade48e2d55663763903a66b974f6faf4c20`, recorded separately. Display returned to 1080x2400/density 420/font scale 1.0, the emulator was stopped and ADB has no devices. Evidence: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl018-20260910-evidence.json` (SHA-256 `a10c92daa4e279c5eed29c450d8512703d55f51b1ead821266db56080a1985ab`). Temporary files created for this leaf were removed after evidence capture; protected user reports remain untracked and unchanged.
- Status: `completed`; local commit: `61ca806` (`fix(knowledge): guard learning path analysis lifetime`).

### Completed Leaf DL-017 - Ingestion analysis ownership and route-exit recovery

- Baseline: `d833c2a`, clean tracked files with only the two protected user reports untracked. No Flutter, Dart or emulator process was running.
- Goal/output: one text/image analysis request per mounted screen; repeated taps during API-key lookup or AI work are ignored; route exit before any awaited stage completes performs no UI/provider/navigation/database work; mounted failures remain visible and retryable. A short 840x1100/200% loading view remains readable without overflow.
- Pre-fix reproduction: the unchanged screen accepted two pending analysis taps (two API-key calls instead of one), allowed a late API-key result to call `setState` after dispose, and its short large-text loading view overflowed by 28 pixels after the lifecycle fix exposed the configuration.
- Implementation: `_analyze` now acquires `_isAnalyzing` before the API-key await, checks `mounted` after key and AI awaits, and `_analyzeTextWithSources` checks route lifetime after extraction, question generation and citation precheck. The loading view uses a scroll-safe constrained layout; source construction, AI task ordering, review navigation and persistence semantics are unchanged.
- Local checks: 4 focused lifecycle checks and 31 related ingestion/import/first-run checks passed. Full `flutter test --no-pub`: 538 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two Dart files with zero changes; `git diff --check` passed.
- Device acceptance: Android API 36 on `xunqiu64_api36`, density 420, normal 1080x2400/1.0 and short 840x1100/2.0. A delayed synthetic API-key service showed loading, duplicate-entry blocking, route exit before the delayed result, missing-credential error and retry entry. The post-fix short loading view has no overflow; filtered logcat recorded zero fatal exceptions, Flutter errors, RenderFlex overflows, unhandled Dart exceptions or blocked database accesses.
- Restoration/evidence: ordinary `lib/main.dart` debug APK SHA-256 `58c5da9e5c4703f23441d3719848c729ea19874c86f3e1cd0fd456680c347f27`; installed hash matched and no DL-017 fixture markers remained. The device database hash was `f88ede786aabec0e644e7517878e9a1fb306ad7c68a947aa82eb27428a0af7da` before and after ordinary APK restoration. Display returned to 1080x2400/density 420/font scale 1.0, the emulator was stopped and ADB has no devices. Evidence: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl017-20260910-evidence.json` (SHA-256 `c1c4868dd6033a69dbde3fe60a1b8e1b18dd57916adc33777d8f7a9ef5089968`) with retained final screenshots, the pre-fix overflow screenshot and filtered log. Temporary files created for this leaf were removed after evidence capture. Both protected user reports retain their recorded SHA-256 values and remain untracked.
- Status: `completed`; local commit: `9d82a32` (`fix(ingestion): guard analysis route lifetime`).

### Completed Leaf DL-016 - Project import picker ownership and route-exit recovery

- Baseline: `5a481d343b60c4f45589c7c48d7d4cb7e1bff1df`, clean tracked files with only the two protected user reports untracked. No Flutter, Dart, emulator or fixture process is running.
- Goal: make project directory/ZIP selection acquire scanning ownership before opening the picker, block duplicate picker calls, and ignore a picker result after the import route has exited. Picker cancellation and picker errors must clear scanning state or expose the existing retryable scan error without changing source scanning or persistence semantics.
- Inputs: `_pickProjectDirectory` and `_pickProjectZip` in `lib/features/ingestion/project_import_screen.dart` call the picker before `_isScanning` is set; `_pickProjectZip` does not check `mounted` before passing a selected file to `_loadProjectSnapshot`, whose first operation is `setState`. Existing widget coverage checks first-run/material-only save but has no pending-picker, throw, duplicate or route-exit ZIP/ directory coverage.
- Output: one pending directory/ZIP picker per screen; pending state disables both source-picker buttons and shows progress; cancellation releases ownership; picker throw shows `项目扫描失败: ...` and permits retry; route exit before picker completion causes no scan, setState or database access. Android directory bridge and non-Android FilePicker paths retain their existing selection and scan behavior.
- Owned files: `lib/features/ingestion/project_import_screen.dart`, new `test/project_import_picker_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary device helpers/logs/evidence use the `anchor-dl016-20260910-01a081db-` prefix outside the repository.
- Acceptance: first prove duplicate pending ZIP invocation, picker throw recovery and late valid ZIP completion failure on the unchanged screen; add focused widget coverage for ZIP, directory bridge, cancellation, retry and route exit. Run focused import/first-run checks, full Flutter tests, analysis, formatting and `git diff --check`. Because this is a UI behavior change, verify real Android picker entry/return and readable progress/error controls at normal and narrow/200% text using synthetic scan/persistence services with database access blocked; retain screenshots, restore the ordinary APK/display settings and database baseline, stop the emulator and remove only this leaf's temporary helpers.
- Boundary: no project scan policy, archive parser, Android directory bridge protocol, ingestion persistence, model request, release operation, credential handling or automatic wake-up change.
- Pre-fix reproduction: the unchanged screen allowed duplicate pending ZIP picker calls, left picker errors without the visible retryable scan error, and continued a valid ZIP result into scanning after route exit. The four focused regressions now pass.
- Implementation: both picker entry points acquire `_isScanning` before opening the picker, block duplicate source-picker calls, release ownership on cancellation, surface picker/scan failures through the existing retryable error state and check `mounted` before late scan/event/provider work. Scan policy, archive parsing, directory bridge protocol and persistence are unchanged.
- Local checks: 4 focused picker checks and 22 related import/first-run/service checks passed. Full `flutter test --no-pub`: 534 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two Dart files with zero changes; `git diff --check` passed.
- Device acceptance: Android API 36, density 420, normal 1080x2400/1.0, large 840x2400/2.0 and short 840x1100/2.0. Real ZIP and directory DocumentsUI entry/return, cancellation, delayed scan progress/success and retryable picker/scan error states passed. The fixture blocked database calls; filtered logcat recorded zero framework/overflow/unhandled-Dart/blocked-database matches. The database hash stayed `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f` before and after.
- Restoration/evidence: ordinary `lib/main.dart` debug APK SHA-256 `10b2139cda360bb42cdfb553caf3ceeccf4848349d146408b5bdb759c81e5259`; installed hash matched and no `dl016_fixture` marker remained. Display returned to 1080x2400/density 420/font scale 1.0, the two DL-016 device ZIPs were removed, and the emulator was stopped. Evidence: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl016-20260910-evidence.json` (SHA-256 `2ddde9d08278deed4a26c5fcc8d8424ce5efd67ebd96f9d637aeb86925da5b9a`) with 43 retained screenshots; 48 temporary files were hash-recorded and removed. Both protected user reports retain their recorded SHA-256 values and remain untracked.
- Status: `completed`; local commit subject: `fix(ingestion): guard project picker ownership`.

### Completed Leaf DL-015 - Privacy restore picker ownership and error recovery

- Baseline: `c575b4cf3c24ecec57bff4765b5b69817d3f37f1`, clean tracked files with only the two protected user reports untracked. No Flutter, Dart, emulator or fixture process was running.
- Goal/output: a restore tap acquires operation ownership before opening the native file picker, repeated taps cannot start overlapping picker/restore flows, picker failures use the visible retryable restore error path, and late picker completion after route exit performs no restore or UI update. Confirmation, provider-owned refresh, backup format and storage semantics remain unchanged.
- Owned files: `lib/features/settings/privacy_data_screen.dart`, `test/privacy_data_screen_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary fixture material used the `anchor-dl015-20260910-01a081db-` prefix outside the repository and was removed after acceptance.
- Pre-fix reproduction: the unchanged screen allowed two pending picker calls from two taps (observed calls 2, expected 1) and let a picker `StateError('picker unavailable')` escape without a visible restore failure. The existing route-exit guard already prevented a late picker result from invoking restore.
- Implementation: `_restoreDatabaseBackup` now returns when any operation is busy, sets `_isRestoring` before `FilePicker.pickFile()`, and keeps selection, confirmation, restore and snackbar handling inside one `try/finally`. The finally block clears busy state only while mounted; no service, schema, file format or provider refresh code changed.
- Local checks: 22 focused privacy-screen checks and 37 related privacy/backup checks passed. Full `flutter test --no-pub`: 530 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format checked two files with zero changes; `git diff --check` passed. One initial combined command named nonexistent `test/local_data_deletion_service_test.dart` and exited 1; the corrected existing-file command passed all 37 checks and is the counted result.
- Device acceptance: Android API 36, density 420, normal 1080x2400/1.0, large 840x2400/2.0 and short 840x1100/2.0. Real DocumentsUI selection, confirmation, synthetic delayed restore, picker cancellation/retry and short-screen confirmation cancellation passed. The fixture blocked database calls; no blocked access, framework error, overflow or zone-error match was observed. The device database stayed `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f` before and after. Evidence and screenshots: `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852\anchor-dl015-20260910-evidence.json` and its `anchor-dl015-20260910-*.png` files.
- Restoration/cleanup: rebuilt ordinary `lib/main.dart` debug APK SHA-256 `495978eee448aacfc18f5a59de51adc8a6c4504af741021aa9d2306c3cc43c43`; installed hash matched, no `dl015` marker remained, display returned to 1080x2400/density 420/font scale 1.0, and ADB has no devices. Exactly 45 temporary files with the DL-015 prefix were hash-recorded and deleted. Both protected user reports retain their recorded SHA-256 values and remain untracked.
- Status: `completed`; local commit subject: `fix(privacy): guard restore picker ownership`. Root reassessment leaves Private Alpha `HOLD` with `cohort_pending`, `release_day_acceptance_primary_stale` and `physical_device_evidence_stale` unchanged.
- Scheduling: `OFF` at the user's request on 2026-09-08. The app deletion API for heartbeat `anchor` ("Anchor 持续开发主任务") returned `deleteStatus=not_found` and "Automation already does not exist in the app." The prior 30-minute schedule is inactive; do not recreate it or replace it with a cron, heartbeat or background project runner. Other projects' automations are unrelated.
- Historical handoff: the original `anchor` heartbeat was moved to task `01a07e0b-378b-78a2-9041-605bca5892b6` through the app API and its TOML was verified at that time. Its backup paths and enabled-state checks are historical evidence, not proof that the automation or backup files still exist now.
- Scheduled wake-ups at 06:05 and 06:41 Asia/Shanghai failed with HTTP 400 before a new leaf began. The original repair below allowed development to resume; DL-004 and DL-005 have since completed. Later `thinking_signature_invalid` and nginx 405 responses are separate unresolved upstream failures. The user redirected work to the project, so further upstream diagnosis is deferred.

### Manual Development Mode (2026-09-08)

- Goal/output: follow the user's request to stop automatic wake-ups and keep subsequent work focused on Anchor in the current task.
- Baseline/input: `5f51088`, clean tracked files; the current development documents still described an active heartbeat. The app API now confirms that `anchor` does not exist.
- Owned files: `docs/DEVELOPMENT_LOOP.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: remove current instructions to schedule or recreate automatic wake-ups, retain completed application evidence and release boundaries, verify local links and `git diff --check`, and preserve the two protected user reports.
- Status: `completed`. The three current documents now require direct task messages for continuation and prohibit automatic re-enablement. All 10 local links resolve, `git diff --check` passes, and both protected-report SHA-256 hashes match the recorded baseline. This documentation-only update does not change the completed application evidence; Flutter/Web checks were not repeated. No temporary files were created or deleted; pre-existing reports and evidence remain retained.

### Continuation transport repair (2026-09-08)

- The 06:41 failure is tied to an unpaired `function_call_output` named `codex_app.automation_update`: Desktop 0.153.4 inserts the heartbeat with no `call_id`, and the configured Responses upstream rejects `input`. A small synthetic request reproduced the same 400; the identical heartbeat as an external message completed successfully on the same model/provider. The issue reproduces without a long conversation or a subagent.
- Installed a local compatibility relay at `D:\Agent\codex\tools\responses-compat-proxy`. It converts only this named, unpaired `<heartbeat>` event into a user-role message, retaining its full text and order. It leaves ordinary tools, model selection, reasoning settings, authentication, upstream errors and stored session history unchanged. Historical heartbeat entries are normalized when resent, so a failed heartbeat need not require another new task.
- Codex user configuration now uses `http://127.0.0.1:18317/v1`; the relay forwards to the same existing upstream at port 8317. Only `model_providers.custom.base_url` changed. Backup: `D:\Agent\codex\backups\config.toml.20260908-072032-heartbeat-compat.bak`, with SHA-256 verified. Context limits and credentials were not changed.
- The relay runs outside the desktop process under the limited current-user logon task `CodexResponsesCompat`; its hidden Node process was started and its health endpoint verified. This task starts the local transport at login and does not wake the project. The separate `anchor` development heartbeat is now absent as recorded above.
- Verification: 13 local transport checks passed, including normal tool preservation, gzip/deflate/Brotli/zstd input, streaming without buffering, cancellation, error passthrough and no automatic replay. A live sequence of heartbeat → ordinary follow-up → another heartbeat completed with exact expected answers, HTTP 200 and `response.completed` for all three requests on `gpt-6-astra`. A fresh, read-only, ephemeral Codex process then used the saved configuration, returned `CONFIG_READY` and exited 0; relay counters confirmed the added traffic. Evidence is in the relay's `live-verification.json` and `repair-evidence.json`; these are transport checks, not Flutter or release acceptance.
- Desktop reload is now verified: this task's request log entries `55893429`, `55893609` and `55893623` use `http://127.0.0.1:18317/v1/responses` (timestamps 1788834913–1788834940). This replaces the earlier port-8317 observation; no further restart is requested for this repair. The relay's separate startup task remains responsible for the transport across desktop restarts.
- Independent rate limiting was observed in one synthetic probe as an SSE `error` with `rate_limit_exceeded` despite HTTP 200. That attempt was not counted as success; the later three-request sequence above completed. The compatibility relay does not remove upstream rate limits or change retry policy.
- Application code was unchanged in the transport repair. Preserve its 406-test DL-003 baseline as dated evidence; the newer DL-004 Flutter checks below belong to the separate product leaf.

### Completed Leaf DL-001 — Checkpoint header readability

- Commit: `575b4dd`. Root workflow setup: `4db0ac9`.
- Goal/output: keep the complete title, phase label and delete action readable and operable at 390px / 100% and 320px / 200%, without splitting `Session` inside the word.
- Inputs: `_AgentResumeCheckpointCard`, the existing unknown-tool-outcome fixture and `anchor-checkpoint-20260908-large.png`. The title, phase and delete action currently compete in one row.
- Owned files: `lib/features/agent/agent_home_screen.dart`, `test/agent_home_navigation_widget_test.dart`, this state document and the current-queue summary in `NEXT_STEPS.md`.
- Preserve: user-facing strings, goal/message wrapping, callbacks, routes, checkpoint state and database behavior.
- Acceptance: a regression that fails on the original narrow/large-text layout; targeted Agent tests; full Flutter tests; analysis and formatting; debug APK and real emulator screenshots at both configurations; primary/cancel and delete actions; App-PID framework logs; `git diff --check`.
- Status: `completed` on 2026-09-08. The word-level regression failed on the original 320px / 200% header. The title now has its own row, with phase/delete below it; both `Agent` and `Session` remain whole words at both tested scales.
- Verification: 18 targeted Agent tests and 401 full Flutter tests passed, zero failures. Analysis: no issues found. Dart format: two files checked, zero changes. Debug APK build and `git diff --check` passed. The existing readability tests were strengthened, so the test count did not increase.
- UI acceptance: the newly built APK ran on Android 16 / API 36 at 1080x2400 / 100% and 840x2400 / 200%, density 420. The complete header, phase, goal/message, metadata and primary button were visible; `Session` occupied one line at 200%. The decision dialog opened and "稍后处理" returned without changing revision 1. The delete action opened confirmation and removed the synthetic session.
- Fixture boundary: reused `_unknownOutcomeCheckpoint(_plan())` through the production codec with unique ID `ui-checkpoint-dl001-20260908-01a07cfd`. A temporary exporter passed its readiness/message checks. State and trace counts were 0 before injection, 1 during acceptance, and 0 after UI deletion; SQLite `quick_check=ok`. No source import or model request was performed.
- App PID 3778: `zoneMismatchLines=0`, `layoutOverflowLines=0`, `frameworkErrorLines=0`, including the final check after dialog, deletion and display restoration. Restored 1080x2400, density 420 and font scale 1.0; shut down the emulator and confirmed ADB has no devices.
- Retained screenshots under `D:\Agent\codex\visualizations\2026\09\07\01a07cfd-0714-73c0-bc13-a10f2bdf8784`: `anchor-loop-dl001-20260908-normal.png`, `anchor-loop-dl001-20260908-large.png`, and `anchor-loop-dl001-20260908-large-dialog.png`.

### Completed Leaf DL-002 — Search and validation architecture accuracy

- Commit: `7bffde1`.
- Baseline: `575b4dd`, clean tracked files after DL-001. The two protected user reports remain untracked and unchanged.
- Goal/output: describe the implemented search paths and question-review boundary accurately so the next root assessment does not plan from nonexistent BM25/embedding or automatic-verification behavior.
- Inputs: `KnowledgeSearchService._score`, `HybridKnowledgeSearchService.search`, search providers/preferences, both import screens, `CitationVerificationTask`, `QuestionValidator` and `SourceGroundedIngestionService`.
- Owned files: `docs/architecture/SYSTEM_OVERVIEW.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: every corrected claim matches its cited symbol; distinguish default lexical search, optional query expansion/RRF and lexical answer context; distinguish model citation precheck, project-only local quality warnings and final review status. Check local links and `git diff --check`. No product code changes or repeat full test runs for this documentation leaf.
- Status: `completed` on 2026-09-08. Corrected the scoped sections and reviewed the diff against the call sites. Text import proceeds directly from citation precheck to review; project import adds local quality warnings. Citation verification asks the model about support but does not copy its reason into the explanation.
- Verification: 39 local Markdown/code references resolve; fenced blocks are balanced; `git diff --check` passes. Confirmed the unclosed historical code fence in `NEXT_STEPS.md` existed at `575b4dd`, and closed it without reconstructing its unfinished example. Only the three owned documentation files changed, so the passing Flutter/Web baselines remain applicable and were not repeated.

### Completed Leaf DL-003 — Search and answer evidence consistency

- Commit: `5d0d579` (`fix(search): align answer evidence with expanded results`).
- Baseline: `c6503ce`; only this task's handoff update to this state document was present before the leaf. The two protected user reports remain unchanged and untracked.
- Goal: reproduce the original-query/expanded-query discrepancy and, if confirmed, select answer evidence from the same completed search branch shown by the knowledge library.
- Inputs: `knowledgeHybridSearchReportProvider`, `knowledgeAnswerGroundedContextProvider`, the screen's `augmented` result selection, and the synthetic English checkpoint corpus with a Chinese query and fake rewrite provider. Existing widget tests override answer context independently and do not exercise this integration.
- Output: a source-backed rewrite hit can enable the answer action with its real local chunk, source, locator and quote boundary; ordinary local results remain usable while a rewrite is pending, disabled or failed. No live model call is needed for this regression.
- Owned files: `lib/core/providers/providers.dart`, `test/hybrid_knowledge_search_service_test.dart`, `test/knowledge_base_search_widget_test.dart`, `docs/architecture/SYSTEM_OVERVIEW.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: prove a provider/widget regression fails on the original implementation; verify augmented evidence, default-off behavior, pending/failure fallback and preference changes; run targeted search/context/widget tests, full Flutter tests, analysis, formatting and `git diff --check`. Preserve existing citation and source validation. The product change is provider wiring; no screen layout change is planned.
- Pre-fix reproduction: two provider regressions failed (rewrite-only context was empty; completed expansion did not add its chunk), while the four original service tests and default-off/fallback checks passed. The real-provider widget regression also failed: the rewritten source was visible and the augmented badge was present, but the answer button callback was null.
- Status: `completed` on 2026-09-08. The context provider observes the report state and chooses the same `augmented` branch as the screen, retaining the immediate lexical path otherwise. Both search consumers share the existing query-family report, so selecting context does not issue a second rewrite request.
- Verification: 22 targeted search/context/widget/adapter/preference tests and 406 full Flutter tests passed with zero failures. `flutter analyze --no-pub --no-fatal-infos`: no issues. Dart format: three files checked, zero changes. Local Markdown links: 10 checked, none missing. `git diff --check` passed.
- Regression evidence: a Chinese query with no local lexical hit now selects the rewritten checkpoint chunk with the original source, trust level, locator and exact-text boundary. A pending rewrite leaves existing local evidence executable; completion, opt-out and opt-in update the selected chunks. Default-off makes no rewrite call; failure retains the lexical evidence. The widget regression uses real search/context providers and verifies the visible hit, augmented badge, enabled answer action and one available source chunk. No live request or persistent data write is involved.
- Test harness note: an initial default-off provider test timed out without an active subscription when a dependency completed. It now holds the same subscriptions as the UI; the pending-rewrite regression separately requires local context to return before the controlled rewrite future completes. All final checks above passed after that correction.

### Completed Leaf DL-004 — Query-scoped answer completion

- Commit: `961d249`.
- Baseline: `d1e30f2a613cd68a6972f9eb9a5bcb985e581015`, clean tracked files on `codex/anchor-web-demo` (ahead 55). No Dart, Flutter, Gradle or emulator process was running at entry. The two protected reports remain untracked with their recorded SHA-256 hashes unchanged.
- Goal: verify and, if reproduced, prevent an answer or record completion from a superseded query/request from altering the current answer panel or starting an obsolete record write.
- Inputs: `_KnowledgeSearchTabState._setQuery`, `_answerQuestion`, `_recordKnowledgeAnswer` and the existing search widget tests. Editing immediately clears answer state but delays `_query`; completion currently checks only that committed string. Returning to the same text and starting a new answer also needs distinct request ownership.
- Output: query edits immediately invalidate pending answer work; the current committed query remains answerable after debounce. Stale success/failure results and record acknowledgements cannot change the new answer. A record write already started for an accepted answer may finish for that original answer, without changing the current panel.
- Owned files: `lib/features/knowledge_base/knowledge_base_screen.dart`, `test/knowledge_base_search_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Any emulator harness is temporary and uses fake tasks, synthetic evidence and an in-memory repository; it must not use live model credentials or stored user data.
- Acceptance: first demonstrate failure on the original implementation using controlled answer/save futures; cover completion before/after debounce, query A → B → A, current retries and disposal. Run focused search/answer/context checks, full Flutter tests, analysis, formatting and `git diff --check`. Verify the affected flow in the real Android emulator at normal and narrow/200% text sizes, retain screenshots and restore display overrides. Preserve existing source/citation validation and search timing.
- Pre-fix reproduction: the unchanged application failed 6 of 16 search widget checks (10 passed). An answer completed before debounce started one obsolete record write; the pending-query answer button stayed enabled; A → B → A revived the first answer; an old failure appeared on the new query; and both successful/failed earlier saves overwrote the saving state of a newer answer for the same text. Completion after debounce, clearing/disposal, current generation retries and current record retries passed.
- Status: `completed` on 2026-09-08. Added per-answer request identity, invalidated on each input edit and new generation; answer and record callbacks require that identity. The answer action waits until draft and committed queries match. Already-started saves still finish for their original record, and only mounted widgets refresh their providers. All 16 search widget checks and 21 focused service/context checks pass. Full `flutter test --no-pub --reporter expanded`: 417 passed, exit 0. `flutter analyze --no-pub --no-fatal-infos`: no issues. Format: two files unchanged; `git diff --check` and the ordinary debug APK build pass. Local commit subject: `fix(search): isolate answer completion across query edits`.
- Temporary materials: removed this leaf's completed full-test and emulator logs after retaining results. Keep `C:\Users\zhang\AppData\Local\Temp\anchor-dl004-20260908-01a07e0b-ui.dart` and `anchor-dl004-20260908-01a07e0b-ui.py` only for the immediately following header acceptance; these isolated fixtures use the actual screen/theme, controlled fake answers and in-memory records. Delete them when that verification finishes. Dependency/build caches and older materials remain retained. No test/build command or emulator is pending.
- Device progress: normal-size interaction confirmed that editing `checkpoint` to `recovery` and then completing the old answer within the 300ms debounce starts zero saves; the action is disabled during debounce and enabled afterward. The current `recovery` answer then saves exactly once. Normal-ready/answer screenshots and a 320px/200% screenshot are retained in the current task's visualization directory. The large-text screenshot exposes pre-existing metric-card overflow and clipped tab/context labels; no layout code was changed by DL-004. This is the next concrete UI candidate, not a passing global layout gate.
- Device interruption/recovery: two headless emulator runs exited with Windows Application Error 1000, `qemu-system-x86_64-headless.exe`, exception `0xc0000005`, at 10:42:53 and 10:56:41 Asia/Shanghai. A read-only diagnostic subagent was interrupted before returning evidence and is no longer active. The documented `-qt-hide-window -no-snapshot -gpu host` launch mode completed the remaining checks and exited 0 on explicit shutdown. At 320px/200%, the old answer again made zero saves and the current answer saved once; completion of an earlier same-query save left the new answer in its own pending-save state until its acknowledgement. Scrolling exposed the correct current answer and citation. The four layout classes were byte-for-byte equal to the baseline, confirming the header issue predates this fix.
- Device cleanup/evidence: restored 1080x2400, density 420, font scale 1.0; rebuilt and reinstalled the normal production entrypoint debug APK; stopped the fixture and emulator and confirmed ADB has no devices. The emulator database SHA-256 remained `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`. The final app-PID log snapshot after display restoration had no zone/overflow matches, but it does not negate the captured earlier layout defect. Five screenshots plus `anchor-dl004-20260908-evidence.json` are retained under `D:\Agent\codex\visualizations\2026\09\07\01a07e0b-378b-78a2-9041-605bca5892b6`. No live model, persistent fixture write or physical-device acceptance is claimed.

### Completed Leaf DL-005 — Knowledge-library large-text readability

- Commit: `2fb155a` (`fix(search): keep library controls readable at large text sizes`).
- Baseline: `961d249`, clean tracked files on `codex/anchor-web-demo` (ahead 56). No Flutter or emulator process remains; protected reports and historical worktrees are unchanged.
- Goal/output: expose the complete knowledge-library metric labels, tab names and source-context count at ordinary size and 320 logical pixels/200% text, with no overflow in empty, ready and answered search states. Tabs remain reachable, answers remain scrollable, and existing query/save behavior is preserved.
- Inputs: DL-004 normal/large screenshots, the 76px `_MetricTile` row, fixed-width five-tab bar, single-row `_KnowledgeAnswerActionBar`, and search layout constraints. The four relevant layout classes were unchanged from the pre-DL-004 baseline.
- Owned files: `lib/features/knowledge_base/knowledge_base_screen.dart`, `test/knowledge_base_search_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: reproduce clipping/overflow with the real production theme at 390px/100% and 320px/200%; assert full labels, reachable final tab and answer/retry controls with empty and executable evidence, including a completed answer. Run search widget and related navigation/error tests, full Flutter tests, analysis, formatting and `git diff --check`. Inspect real Android emulator screenshots/interactions at both sizes; restore display values and ordinary debug APK; verify original database hash and stop emulator. Keep fixtures separate from model/physical-device/release evidence.
- Pre-fix reproduction: 3 of the 4 new production-theme checks failed (17 of 20 total widget checks passed). Normal-size `知识点` was ellipsized; at 320px/200% all four metric columns overflowed vertically by 12px and the no-evidence answer action row overflowed horizontally by 70px. The completed-answer check at normal size passed.
- Status: `completed` on 2026-09-08. Metrics give labels their own row, grow with content, and select four/two tracks from available width and requested text size. Tabs scroll when needed; context text, tools and the answer button wrap across the full search width. Answers share the remaining height with results; both answer and generation-error details scroll. A zero-size initial Android window no longer constructs negative tile widths. Counts, callbacks, tab routes, query timing, request identity and record semantics are preserved. Local commit subject: `fix(search): keep library controls readable at large text sizes`.
- Additional reproductions: the first rebuilt fixture reported a negative minimum tile width (-4px) during initial zero-size layout. The new startup regression failed before the guard and passed afterward. The limited-height generation-error branch later overflowed by 28px on the device and 51px in a 320x740/200% widget check; replacing its fixed padding surface with a scroll view resolved that failure. Earlier 421/422-test passes preceded these final corrections and are superseded by the results below.
- Final verification: 38 focused search/navigation checks and 423 full Flutter tests passed, exit 0. Analysis: no issues. Dart format: two files, zero changes. `git diff --check`, fixture APK and ordinary-entrypoint debug APK builds passed. One earlier focused command named a nonexistent test file and exited 1; only the corrected suites are counted. No Web change or repeat Web run was needed.
- Device acceptance: the real Android 16/API 36 emulator at 1080x2400/100% and 840x2400/200%, density 420, shows complete metric/context labels. The final tab is reachable by horizontal scrolling and opens the pending-verification page; source navigation, refresh and copied-diagnostic confirmation work with no answer evidence. Normal and large-text generation failure/retry save one current synthetic answer per successful retry. Answers, source locators and error retry context can be scrolled into view; clear returns to history. A real keyboard leaves the search input/action visible and generation completes without framework errors. Final fixture PID 4677 reported zero retained framework errors and zero zone/overflow log matches after large and normal retries.
- Device restoration/data: restored 1080x2400, density 420 and font scale 1.0; stopped the fixture, rebuilt and installed the ordinary debug APK, verified its kernel does not contain the temporary service extension, and explicitly shut down the emulator (exit 0). ADB has no devices and no matching Flutter/emulator process remains. Before and after ordinary-APK restoration, the database hash was `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`, matching the retained DL-004 baseline; WAL and SHM were absent. An early cold-boot probe had reported the package unavailable; its cause is unconfirmed, and that probe is not evidence of data loss. No AVD data or snapshots were deleted.
- Evidence/cleanup: retained 18 screenshots, including clearly named intermediate/before-fix states, and `anchor-dl005-20260908-evidence.json` in the current task's visualization directory. The JSON records final checks, device observations, ordinary-APK hash, database comparison and cleanup hashes. Removed the two retained DL-004 Dart/Python fixtures and the DL-005 full-test/emulator logs after recording results. All answer records were synthetic and held in memory; this is not live-model, physical-device or release acceptance. Protected user-report hashes remain unchanged; historical worktrees, older evidence and build/dependency caches remain retained.

### Completed Leaf DL-006 - Quiz submission ownership

- Commit: `0c54f2b` (`fix(quiz): isolate submissions and keep completion readable`). Recovery checks confirmed both source hashes, the evidence JSON and 10 local documentation links before committing only the four owned files.
- Baseline: `636440d`, clean tracked files on `codex/anchor-web-demo` (ahead 59); the two protected reports remain untracked. No Flutter or emulator process was running at entry.
- Goal: prevent repeated answer/finish actions from awarding XP, deducting hearts, updating review/mastery or saving a completed session more than once while the original operation is pending.
- Inputs: `QuizScreen._checkAnswer`, `_nextQuestion`, `_finishQuiz` and the answer widgets; `_isChecking` currently covers only AI judging, not subsequent local writes, and completion has no in-flight guard. Existing practice-target tests only open and close this screen.
- Output: one answer submission per question, stable submitted input until persistence finishes, and one completion sequence per quiz. A stale action must not skip the next question; disposed screens must not receive UI callbacks from these operations. Preserve scoring, verification filters, scheduling formulas, source/citation validation and storage schema.
- Owned files: `lib/features/learning/quiz_screen.dart`, `test/quiz_submission_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Emulator fixtures use only synthetic questions and in-memory fakes outside the repository and are removed after acceptance.
- Acceptance: reproduce duplicate effects with controlled futures on the original implementation; cover local/AI answer delays, repeated completion, transition to the next question and screen disposal. Run focused quiz/practice/golden-path checks, full Flutter tests, analysis, formatting and `git diff --check`. Verify pending controls and successful progression on the real Android emulator at normal and narrow/200% text, retaining screenshots and restoring the ordinary APK/display settings and database baseline.
- Scope boundary: this leaf does not make the existing database and SharedPreferences writes transactional or redesign storage-failure recovery. It addresses overlapping UI submissions and stale callbacks.
- Device-driven scope extension: normal and 320px/large-text taps confirm exactly one submission/completion, and next-question controls start unselected. The unchanged completion layout emits a 63px right overflow in its perfect-score heart message on the device. Add a focused completion-layout regression and make that message wrap; ensure the result and return action remain reachable at limited height. This stays in the already owned screen/test files and does not alter rewards.
- Status: `completed`. All five initial regressions failed on the original screen: two overlapping checks, mutable submitted selection, two overlapping completions, a skipped second question, and duplicate AI judging with a later answer. The screen now guards the full submission/completion sequence, captures the submitted answer, resets answer-widget identity per question, and avoids stale screen callbacks. Once local writes start they finish through captured services; results awaiting AI when the page closes do not start those writes. Local commit subject: `fix(quiz): isolate submissions and keep completion readable`.
- Final checks: 15 new quiz widget checks plus 11 existing practice/golden-path checks passed (26 total); full Flutter suite 438 passed, exit 0. Analysis: no issues. Format: two files, zero changes. Diff check and both fixture/ordinary debug APK builds passed. The earlier 437-test run preceded the completion-layout fix. Its new 320x740/200% regression reproduced XP-row overflows of 40/68px, heart-message overflow of 108px and a 264px bottom overflow. The final result surface scrolls, reward labels/messages wrap, and metric values fit their existing tiles. One harness test initially left an animation timer pending; advancing its test clock after the result frame resolved that issue.
- Device acceptance: Android 16/API 36, density 420, at 1080x2400/100%, 840x2400/200% and a limited-height 840x1943/200% result view. Three actual taps on check/finish produced one submission/completion; two taps on continue left question 2 unselected. The perfect two-question run saved one 2/2 record and added 120 XP; the mixed run deducted one heart, saved one 1/2 record and added 60 XP. The corrected heart message is fully readable, results scroll, and return works at both sizes. Real keyboard input followed by three checks made one synthetic AI judgment; Android back before its completion left all local write counts at zero. No live AI request or persistent fixture record was used.
- Restoration: final fixture PID 4271 had zero retained framework errors and zero zone/overflow/error log matches. Rebuilt and reinstalled the ordinary debug APK (SHA-256 `b5ac61742cc3fdd52281c1b5198586e2f30fa4376b1e92b096485316629fb6ba`); its kernel does not contain `ext.anchor.dl006`. Restored 1080x2400, density 420, font scale 1.0; removed this leaf's port forward and stopped the emulator (exit 0). ADB has no devices and no matching Flutter/emulator process remains. Database SHA-256 before fixture startup and after ordinary-APK restoration was `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`, matching DL-005. The initial cold-boot package lookup again failed before replacement install; its cause is unconfirmed, and the retained database hash still matched.
- Evidence/cleanup: retained 15 screenshots, classified as intermediate or final, plus `anchor-dl006-20260908-evidence.json` under the current task's visualization directory. Removed the exact temporary Dart fixture and Python controller after checking their recorded hashes. The two protected reports retain their recorded SHA-256 values; historical evidence and worktrees remain untouched. The attempted read-only subagent could not start because the agent-thread limit was reached, so the primary agent performed the inspection directly.

### Completed Leaf DL-007 - Quiz loading recovery

- Commit: `7c9342e` (`fix(quiz): recover question loading and ignore late results`). Only the four owned files were committed; the protected reports remain untracked.
- Baseline: `0c54f2b`, clean tracked files on `codex/anchor-web-demo` (ahead 60); both protected reports remain untracked with unchanged hashes.
- Goal/output: a failed question read exits loading and offers retry; loading/error screens allow return; late success or failure after leaving the screen produces no UI update or unhandled asynchronous exception. A retry starts only one pending read and preserves verified-only practice.
- Inputs: `_loadQuestions` awaits `QuestionRepository.getQuestionsByDeck` without error handling or a mounted check; its loading scaffold has no visible back action. Use controlled synthetic repository futures, the production theme and the existing `KnowledgeLibraryErrorState` for consistent error recovery.
- Owned files: `lib/features/learning/quiz_screen.dart`, new `test/quiz_loading_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: reproduce failures on the original loading implementation; cover success/error after disposal, synchronous read failure, repeated retry, prefetched/empty input and verified filtering. Run focused quiz/practice/golden-path tests, full Flutter tests, analysis, formatting and diff checks. Verify retry/back and readable error controls on the real Android emulator at normal and 320px/200% text, retain screenshots, restore the ordinary APK/display settings and compare the database baseline.
- Boundary: no repository/schema changes, live model requests, storage transaction redesign, release operations or automatic wake-ups. Temporary emulator helpers use synthetic data and are removed after verification.
- Pre-fix reproduction: after correcting a missing required field in the test-only stats fake, the unchanged screen failed 7 of 9 widget checks. Evidence includes an unhandled read error, `setState() called after dispose()`, an unhandled late failure, an unhandled synchronous repository failure and the missing loading back button. Prefetched filtering and no-input empty behavior passed.
- Status: `completed`; loading now guards overlapping reads, catches read failures, checks mounted before UI updates and exposes an error retry. Loading/error states have a back action, and the existing error controls are inside a scrollable surface. Prefetched questions retain their original immediate path and source-status filter.
- Final checks: all 9 new loading checks and 26 existing quiz/practice/golden-path checks passed (35 total); 447 full Flutter tests passed, exit 0. Analysis: no issues. Format: two files, zero changes. Diff check and fixture/ordinary debug APK builds passed. No Web code changed or repeat Web check was needed.
- Device acceptance: Android 16/API 36, density 420, at 1080x2400/100%, 840x2400/200%, and a short 840x1100/200% error view. Three actual retry taps started one read; success displayed only the verified synthetic question. The app-bar back action returned before pending reads completed, and subsequent success/failure left the root page intact. Copied-diagnostic acknowledgement appeared at both scales; the short error surface scrolled to its controls. All 9 screenshots are nonblank; normal/large/short error screenshots were visually inspected. Fixture PID 3546 reported zero retained framework errors and zero zone/overflow/error log matches across 45 recorded observations.
- Restoration: rebuilt/reinstalled the ordinary debug APK (SHA-256 `5f5ed78d6f08331d9b677d616ba4f46e9d7768dbf70557a40596705b8a10d2b5`), verified no `ext.anchor.dl007` in its kernel, restored 1080x2400/density 420/font scale 1.0, removed the temporary forward and shut down the emulator (exit 0). ADB lists no devices and no matching Flutter/emulator process remains. Database SHA-256 before fixture startup and after restoration matched DL-006: `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`. The initial cold-boot package lookup again reported unknown package before replacement install; its cause remains unconfirmed.
- Evidence/cleanup: retained 9 screenshots, `anchor-dl007-20260908-device-evidence.json` and `anchor-dl007-20260908-evidence.json` under the current task visualization directory. Removed the two exact temporary Dart/Python helpers after recording hashes. The first controller run encountered a startup PID race; bounded polling corrected the harness and the next complete device run passed. Protected reports, prior evidence, historical worktrees and build/dependency caches remain retained. The separate read-only storage-audit subagent failed before execution with `model_not_found`; no finding is attributed to it.

### Completed Leaf DL-008 - Atomic quiz saves and retry recovery

- Commit: `741b5c3` (`fix(quiz): make quiz saves atomic and retryable`). The 17 owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `3ca6106`, clean tracked files, resumed from task `01a07e0b-378b-78a2-9041-605bca5892b6`. Its final user message authorized this fix. The previous isolated reproduction observed 20 XP for one correct answer, two heart deductions for one wrong answer, and duplicate completion/perfect rewards after failed downstream writes. The current 24 quiz submission/loading tests pass but do not cover those failures.
- Goal/output: commit each accepted answer and each completion as a single SQLite transaction. Failed writes roll back rewards, review/mastery, counters and records together. A stable operation ID returns the committed result on retry without repeating effects. The screen retains its accepted judgment and exposes retry/back after a save error; callbacks after exit cannot change the disposed screen.
- Owned files: `lib/data/database/database_helper.dart`, new `lib/services/quiz_persistence_service.dart`, `lib/services/gamification_service.dart`, `lib/services/scheduling/review_scheduler_service.dart`, `lib/services/scheduling/mastery_service.dart`, `lib/core/providers/providers.dart`, `lib/features/learning/quiz_screen.dart`, `lib/services/privacy/local_data_backup_service.dart`, `lib/services/privacy/local_data_deletion_service.dart`, new `test/quiz_persistence_service_test.dart`, `test/quiz_submission_widget_test.dart`, `test/database_migration_test.dart`, `test/local_data_backup_service_test.dart`, `test/privacy_controls_test.dart`, `docs/architecture/DATA_MODEL.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Storage boundary: schema v24 adds operation receipts and SQLite-backed cumulative/check-in statistics, with one-time import of the existing preferences. Receipts contain outcome metadata, never answer text or source content. Backup/restore and learning-history deletion must include the new tables. Review formulas, reward amounts, verified-question filtering and latest-per-deck record semantics remain unchanged. A durable receipt supports replay with the same ID; automatic restoration of an unfinished quiz UI is outside this leaf.
- Acceptance: fail new UI recovery regressions against the original screen; inject SQLite failures after rewards/reviews/records and at receipt commit, retry repeatedly/concurrently, and reopen the database to verify replay. Cover legacy counters/check-ins, schema upgrade, backup/restore, history deletion, AI judgment reuse, random practice and disposal. Run focused tests, the full Flutter suite, analysis, formatting and diff checks; verify normal and 320px/200% error/retry interaction on the real Android emulator, preserve screenshots, restore display settings and the ordinary APK, and compare the existing device database hash.
- Status: `completed` on 2026-09-09. The three new recovery regressions failed on the original screen. Answer saves now roll back XP/hearts, counters, check-in, review and mastery together; completion saves do the same for rewards, perfect count, records and deck mastery. Fixed operation IDs make retries return the saved result, and the screen keeps its judgment without calling AI again. Save errors offer retry, back and content-free diagnostics. Local commit subject: `fix(quiz): make quiz saves atomic and retryable`; the final SHA is retained in the evidence summary below.
- Final checks: 82 focused checks and 483 full Flutter tests passed, exit 0. The final full run finished in 37 seconds; analysis reports no issues after resolving six initial style infos. Dart format checked 14 files with zero changes. Fixture and ordinary-entrypoint debug APK builds and `git diff --check` passed. An actual SQLite COMMIT rejection initially left the sqflite connection blocked and caused a test timeout; the shared helper now closes that connection so uncommitted work rolls back and retries reopen it. The final fault-injection checks pass on both FFI SQLite and native Android sqflite.
- Device acceptance: Android API 36 on `xunqiu64_api36`, density 420, at 1080x2400/100% and 840x2400/200%. Each scale completed seven scenarios: correct-answer failure, wrong-answer failure, completion failure, lost acknowledgement, final COMMIT rejection, late save failure after exit, and exit while AI judgment was pending. With synthetic XP 10000/hearts 5/streak 0, correct completion ended at 10110 XP with two receipts and one latest-per-deck record; wrong completion ended at 10050 XP/hearts 4; the exit scenarios retained 10000 XP and zero receipts. Repeated retries used the same operation ID. Error/result controls remained usable, including the scrollable 840x1100/200% error surface. A real keyboard was used with a controlled AI substitute; no live model call was made.
- Isolation/restoration: the real screen, theme and persistence services used a separate file-backed SQLite fixture and mock preferences. Its database was deleted after acceptance, and no fixture database files or device UI XML remain. All 173 observations and 188 retained framework-error snapshots were checked; every error list is empty. Final Anchor PID 3740 logs contained zero zone, overflow or framework-error matches. Restored 1080x2400/density 420/font scale 1.0, removed the temporary forward, installed the ordinary APK (SHA-256 `35df1d1f4403023d60f43dc9b0af30be794f3d4c6b483dc867bf282b9fa3b5e0`) and verified the installed hash and absence of `ext.anchor.dl008` in its kernel. The ordinary entrypoint was not launched against existing data. Database SHA-256 before fixture startup and after restoration remained `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`. The emulator is stopped, ADB lists no devices, and no matching task runtime remains.
- Auxiliary review: the one read-only default subagent returned no findings within its bounded review window and was interrupted. No acceptance claim relies on its output; the primary agent owns the implementation and checks.
- Evidence/cleanup: retained 37 PNG screenshots, `anchor-dl008-20260909-device-evidence.json` and `anchor-dl008-20260909-evidence.json` under `D:\Agent\codex\visualizations\2026\09\08\01a081db-b31d-7693-8ecb-6040d5d44852`. The summary records verification, APK/database hashes, restoration and cleanup. Removed exactly five files under `C:\Users\zhang\AppData\Local\Temp` after recording hashes: prefix `anchor-dl008-20260909-01a081db-` with suffixes `ui.dart`, `ui.py`, `full-test.log`, `emulator.out.log` and `emulator.err.log`. Both protected user-report hashes remain unchanged and the reports stay untracked. Historical worktrees, older evidence/temporary materials and build/dependency caches remain retained.

### Completed Leaf DL-009 - Learning state after a quiz exits

- Commit: `80deb4b` (`fix(quiz): refresh learning state after route exit`). All five owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `741b5c3`; `QuizScreen._checkAnswer` and `_finishQuiz` return after `await` when unmounted, before updating shared statistics or invalidating data providers. Parent routes refresh on navigation return, which can precede the accepted save. Existing late-success tests assert writes and absence of exceptions but do not assert refreshed consumers.
- Goal/output: an accepted answer or completion refreshes shared learning state after it saves, even if the quiz has exited. The closed screen receives no callbacks; closing the entire provider scope remains safe. Refresh from current persisted statistics so an older acknowledgement cannot overwrite newer progress. UI save success remains tied to persistence, not a separate statistics-read success.
- Owned files: `lib/core/providers/providers.dart`, `lib/features/learning/quiz_screen.dart`, `test/quiz_submission_widget_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary device helpers, logs and retained evidence use the `anchor-dl009-20260909-01a081db-` prefix outside the repository.
- Acceptance: first fail regressions with controlled save futures after a parent refresh/exit; cover XP/hearts, review/question/knowledge state and completion records/counters, an older saved result, scope disposal and stats-read failure. Run focused quiz/provider-related checks, full Flutter tests, analysis, formatting and diff checks. Verify real Android interaction at normal and 320px/200% text with synthetic data, preserve screenshots, restore the normal APK/display and existing database hash, stop the emulator and remove this leaf's temporary helpers.
- Boundary: retain schema v24, transactional/idempotent saves, reward/scheduling formulas, source validation and the existing route results. No live model, release action or automatic wake-up is involved. The read-only route audit returned no usable result within its bounded window and was interrupted; the primary agent inspected the relevant calls directly.
- Pre-fix reproduction: five new regressions failed, with 27 checks passing. After parent return/refresh, correct answers left XP 10000 instead of 10010 and all review/check-in/mastery consumers stale; wrong answers left hearts 5 instead of 4; completion left XP 10010, perfect count 0 and no visible record. Older answer/completion acknowledgements replaced newer XP 12000 with 10010/10110. Scope-disposal and refresh-error boundary checks passed on the baseline.
- Status: `completed` on 2026-09-09. Provider-owned quiz operations refresh current shared data after save, independently of route lifetime. Statistics refresh runs separately from the acknowledged save; accepted writes may finish after the provider scope closes, without refreshing that closed scope. An additional refresh-failure test exposed the completion screen reading `AsyncError.value`; it now renders the saved completion snapshot, retaining its original reward breakdown. Parent-cache tests hold actual provider subscriptions so they exercise visible consumers instead of inspecting a lazily restarted provider before its future resolves. Local commit subject: `fix(quiz): refresh learning state after route exit`.
- Local checks: 81 focused checks and 495 full Flutter tests passed, exit 0; the full run finished in 31 seconds. Analysis reports no issues after removing a redundant null comparison; three Dart files have unchanged formatting and `git diff --check` passes. The fixture APK builds. Its initial helper-only build failed on a mixed provider list inferred as `Object`; explicit invalidation calls corrected that temporary helper.
- Device acceptance: Android API 36, density 420, at 1080x2400/100% and 840x2400/200%, completed five scenarios per scale (10 total). Returning before correct/wrong saves finished now refreshes XP/hearts, due questions, mastery, correct count and check-in. Late completion refreshes its record and perfect count. Both old answer/completion receipts preserve newer XP 12000; the completion view still shows its original 110 XP breakdown rather than a newer streak bonus. Each answer/completion was called once. All 127 observations and all three app-PID log snapshots had zero framework/zone/overflow errors; the final fixture PID was 5269. The screen/theme/providers are real; persistence and repositories use synthetic in-memory replacements, with access to the real database blocked. This leaf does not claim new native SQLite or live-model acceptance.
- Harness corrections: run1 failed to locate Flutter content descriptions; run2 passed all five normal cases, then a large-text tap hit the empty gap inside the merged path-node bounds. Direct XML, screenshot and widget inspection confirmed that location. Run3 targeted the visible circle and passed all five missing large-text cases without repeating the normal cases. Two startup-retry port forwards were identified through the retained fixture-PID VM ports and removed individually. Intermediate failures remain in the evidence and are not counted as complete runs.
- Restoration/evidence: rebuilt and installed the ordinary debug APK (SHA-256 `b9aa197cbca97c053532278c2d79946ab05bcbbcf9767404cc70e9d6bda30093`), verified its installed hash and absence of `ext.anchor.dl009` in its kernel, and did not start that entrypoint against user data. Database SHA-256 before and after restoration remained `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`. Display is restored to 1080x2400/density 420/font scale 1.0; no temporary UI XML or forwards remain. The emulator is stopped, ADB has no devices, and matching task runtimes are gone. Retained 28 screenshots, three run-device JSON files and `anchor-dl009-20260909-evidence.json` under the current task visualization directory. The summary records source evidence, APK/database hashes, restoration and cleanup.
- Cleanup: removed this leaf's five exact temporary files (`ui.dart`, `ui.py`, `full-test.log`, `emulator.out.log`, `emulator.err.log`) with prefix `anchor-dl009-20260909-01a081db-`, after recording their hashes. Both protected reports retain their baseline hashes and stay untracked. Historical worktrees remain clean; older evidence, temporary materials and build/dependency caches remain untouched. The pre-existing large-text Today Review heading/count truncation is retained as a separate root candidate, not a passing global readability claim.

### Completed Leaf DL-010 - Today Review summary readability

- Commit: `af91df6` (`fix(home): keep today review counts readable`). All four owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `80deb4b`, clean tracked files and no running test/build/emulator. The DL-009 `run2-large-home-inspection.png` shows the banner heading as `今日…`, hiding both knowledge-point and question counts. `HomeScreen._buildTodayReviewBanner` puts its icon, two single-line texts and review action in one row; existing home responsive tests override the review queue to empty.
- Goal/output: show the full Today Review heading and both counts at 390px/100% and 320px/200%, while keeping the review action visible and working. Give the heading enough width and allow it to wrap; retain the existing bounded topic-title preview. Header, counts, action and remaining scrollable home content must not overlap or overflow.
- Owned files: `lib/features/home/home_screen.dart`, `test/home_screen_responsive_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary helpers/logs/evidence use prefix `anchor-dl010-20260909-01a081db-` outside the repository.
- Acceptance: reproduce the clipped nonempty banner with the production theme on the unchanged screen; verify complete rendered heading/counts, reachable review action, real route open/back and preserved ten-question request limit, plus empty/loading/error banner states. Run focused home/quiz/practice checks, full Flutter tests, analysis, formatting and diff checks. Inspect actual Android screenshots and use the review control at normal and narrow/200% text; restore the ordinary APK/display, compare the database baseline, stop the emulator and clean only owned temporary helpers.
- Boundary: presentation-only change to the review banner; preserve labels/count calculations, topic preview, question filtering, provider refresh, storage, rewards and navigation behavior. No broad home redesign, live model call, release action or automatic wake-up. Direct widget/screenshot evidence makes another exploratory subagent unnecessary.
- Pre-fix reproduction: all three nonempty-banner checks failed on the original screen at 390x844/100%, 320x740/200% and 320x600/200%; the five existing/hidden-state checks passed. The first test run also exposed shared mock-preference state leaking into the older random-mode tests; resetting preferences for each test corrected the harness without changing expectations.
- Status: `completed` on 2026-09-09. The complete heading wraps above a separate action row, with the existing topic-title preview beside the icon. All 63 focused home/quiz/practice checks and 501 full Flutter tests pass, including review navigation/back and a request limit of 10; the full run finished in 31 seconds. Analysis found no issues. Formatting checked two Dart files with zero changes; diff check and fixture/ordinary APK builds passed. Only the banner layout changed in application code. Local commit subject: `fix(home): keep today review counts readable`.
- Device acceptance: Android API 36, density 420, at 1080x2400/100%, 840x2400/200% and 840x1575/200%. The complete `今日复习 · 3 个知识点 · 12 题` heading is visible without ellipsis; the review action opens the actual quiz and Android back returns to the home banner at all three configurations. Each request retained the ten-question limit. The short screen can scroll the learning-path caption above the floating action button. All 18 retained observations and both app-PID log snapshots have zero framework/zone/overflow errors; final fixture PID 3891. The first controller checked bounds during the reverse route animation; the completed run waits for that animation before applying the unchanged visibility assertions. Its earlier two screenshots remain classified as intermediate evidence.
- Isolation/restoration: the real home/quiz/theme used synthetic provider values, mock preferences and blocked database/persistence dependencies. No answer was submitted or live model invoked. Rebuilt and installed the ordinary debug APK (SHA-256 `234bb7020d564391e30d7f42774436bf07c3cbce5578c8a90365ec1a318bbc4d`), matched the installed hash and verified no temporary service extension in its kernel. That ordinary entrypoint was not launched against user data. The database hash before fixture startup, before restoration and afterward remains `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`. Display is restored to 1080x2400/density 420/font scale 1.0; no fixture XML or port forwards remain. The emulator is stopped, ADB has no devices, and matching task runtimes are gone.
- Evidence/cleanup: retained 12 screenshots, two device-run JSON files and `anchor-dl010-20260909-evidence.json` in the current task visualization directory. Removed all five exact temporary files with prefix `anchor-dl010-20260909-01a081db-` and suffixes `ui.dart`, `ui.py`, `full-test.log`, `emulator.out.log`, `emulator.err.log` after recording hashes. Both protected reports keep their baseline hashes and remain untracked. Earlier evidence, temporary materials, historical worktrees and build/dependency caches remain retained. Bounded topic-title previews are unchanged and are not claimed to show every topic title in full.

### Completed Leaf DL-011 - Review scheduling architecture accuracy

- Commit: `6cbe330` (`docs: align review scheduling with current persistence`). All three owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `af91df6`, clean tracked files. `SYSTEM_OVERVIEW.md` describes +0.1 correct-answer ease, a partial-correct branch, an interval based on elapsed days and per-answer StudyRecord writes. Direct reads of `ReviewSchedulerService`, `MasteryService`, `QuizPersistenceService`, `QuizOperations`, `Question` and `StudyRecord` confirm different current behavior. The architecture document was read in full before choosing edits.
- Goal/output: make the review flow and overall data-flow diagram match current code, including separate answer/completion transactions, shared refresh after route exit, exact review/mastery formulas, verified/due filtering and queue limits. Explain latest-per-deck records and durable receipt replay without implying cross-process quiz-page restoration.
- Owned files: `docs/architecture/SYSTEM_OVERVIEW.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: match each corrected claim to its current symbol; verify formula snippets against source, queue ordering/limits and default-field examples, all local Markdown/code references, balanced fences, scoped diff and unchanged application-code hashes. Retain the applicable 501-test/runtime evidence from DL-010; do not repeat Flutter/Web checks for documentation-only edits.
- Boundary: correct only the scheduling section, its overall-flow counterpart and the document's dated verification note. Other historical designs/performance estimates and release evidence remain outside this leaf. No dependency, automation or external action is needed.
- Status: `completed` on 2026-09-09. The flow now shows separate answer/completion transactions, receipt replay and provider-owned refresh. It documents +0.12/-0.2 ease, the exact interval/mastery formulas, verified/due and knowledge-point filtering, priority and question ordering, the 12-point queue default and the ten-question practice path. StudyRecord is described as the latest completed result per deck. Local commit subject: `docs: align review scheduling with current persistence`.
- Verification: 18 local Markdown links and 22 architecture source-file references resolve; both formula blocks match their current source after whitespace normalization, and three numerical examples match the defaults/weights. Fences are balanced, `git diff --check` passes, and all eight inspected application-source hashes are unchanged. Initial broad path checks incorrectly treated historical locator/glob examples in NEXT_STEPS as real file links; the final check covers actual links and direct architecture source references without rewriting those old examples. No Flutter/Web test or build was repeated for this documentation-only leaf.
- Evidence/cleanup: the commit and source/reference checks are retained in `anchor-dl011-20260909-evidence.json` under the current task visualization directory. No temporary scripts, logs or directories were created or deleted. Protected reports, previous device evidence, historical worktrees and existing caches remain retained.

### Completed Leaf DL-012 - Release-index evidence dates

- Commit: `5c9f1c2` (`docs: date release evidence and link current development`). All four owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `6cbe330`, clean tracked files. OPEN_SOURCE_CHECKLIST and OPEN_SOURCE_READY were last updated on 2026-08-28 but call 386 Flutter / 20 Web checks current; the latter says current code passed a signed release build while identifying an APK from 2026-08-26. Both documents were read in full; their HOLD blockers and dated candidate are already established external-evidence records.
- Goal/output: label the repository-quality counts as the 2026-08-28 snapshot and the signed artifact/device/model checks as the 2026-08-26 candidate evidence. Link CURRENT_STATE for current local results, retaining one development pointer and avoiding a duplicate rolling test count. Make clear that subsequent local changes/debug checks do not refresh signed-release acceptance.
- Owned files: `docs/OPEN_SOURCE_CHECKLIST.md`, `docs/OPEN_SOURCE_READY.md`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`.
- Acceptance: preserve the historical numbers, APK/hash/version/profile and opaque evidence references, all three HOLD blocker codes and outstanding external tasks. Check local links, fences, scoped diff, unchanged application-source hashes and protected-report hashes. No model request, device run, release build, readiness evaluation or repeat Flutter/Web suite is required for this evidence-labeling change.
- Boundary: documentation-only clarification; no changes to published state, release evidence files, signing, credentials or external systems.
- Status: `completed` on 2026-09-09. The two release indexes now label the 2026-08-28 quality snapshot and 2026-08-26 signed candidate, link this single development pointer and avoid copying its rolling test count. Existing governance records retain their original undated descriptions. Local commit subject: `docs: date release evidence and link current development`.
- Verification: all nine local Markdown links resolve; 42 historical metadata comparisons preserve the original test/coverage counts, artifact identity, profile and opaque references. The recorded signed-APK hash in both indexes and all three HOLD blockers are unchanged. Fences and `git diff --check` pass; the eight inspected application-source hashes and both protected-report hashes are unchanged. Only the four owned documentation files changed. The DL-010 application baseline remains applicable; no test, build or readiness evaluation was repeated.
- Evidence/cleanup: commit and comparison results are retained in `anchor-dl012-20260909-evidence.json` in the current task visualization directory. No temporary scripts, logs or directories were created or deleted. The two protected reports remain untracked; previous evidence, old temporary materials, historical worktrees and dependency/build caches remain retained.

### Completed Leaf DL-013 - Privacy data-operation refresh

- Commit: `315860a` (`fix(privacy): refresh local data after operations settle`). All five owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `5c9f1c2`. `PrivacyDataScreen._invalidateDeletedData` omits monthly check-in, medals, total/perfect counts, study records and several learning-history consumers. Restore calls `invalidateDatabaseBackedProviders(ref)` after an await with a route-bound WidgetRef; that helper also omits `allProgrammingExercisesProvider`. Deletion's SQLite transaction can finish before later preference cleanup fails, but its screen only refreshes on success. Existing privacy widget tests check confirmation/messages without subscribing to affected cached consumers.
- Goal/output: after an accepted restore or deletion settles, active consumers read the resulting stored state even when the privacy route has closed. All affected learning/history/statistics caches refresh; partial deletion failures and restore rollback must not leave a stale success-era snapshot. Errors remain errors and callers can retry; disposing the whole provider scope causes no late refresh or UI callback.
- Owned files: `lib/core/providers/providers.dart`, `lib/features/settings/privacy_data_screen.dart`, `test/privacy_data_screen_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary synthetic device helpers/logs and retained evidence use prefix `anchor-dl013-20260909-01a081db-` outside the repository.
- Acceptance: fail subscribed-consumer regressions on the unchanged implementation for deletion/restore while mounted and after route exit; cover partial failures, retry, scope disposal and preserved nonselected data. Run focused privacy/backup/provider-related checks, full Flutter tests, analysis, formatting and diff checks. Verify real Android privacy actions and returning consumers at normal and narrow/200% text using synthetic services with real-data access blocked; retain screenshots, restore the normal APK/display, compare the database baseline, stop the emulator and remove owned temporary helpers.
- Boundary: preserve confirmation and backup-before-delete behavior, file format, SQLite schema, restore/rollback mechanics, deletion scope semantics, credentials and release state. This leaf changes operation/refresh ownership and cache coverage; it does not add a cross-service storage transaction or redesign the file picker.
- Pre-fix reproduction: the unchanged application failed eight of 14 privacy widget checks (six passed). Mounted history/content deletion retained total-correct 12 instead of 0; mounted restore retained the original programming exercise. After route exit, both deletion scopes kept XP 120 instead of 0 and restore kept 120 instead of 240. Partial deletion failure retained pre-delete XP; a restore rollback left a subscribed intermediate count of 99 instead of 12. All checks retain real provider subscriptions across navigation.
- Status: `completed` on 2026-09-09; the provider-owned operation wrapper refreshes final stored state on both success and failure, independently of the route. All 18 privacy widget checks and 63 related privacy/backup/quiz/Agent/programming checks pass (81 total, exit 0). Full Flutter: 515 passed, exit 0; analysis found no issues; three Dart files have unchanged formatting and diff checks pass. All ten Android operation scenarios, fixture/ordinary APK builds, restoration and cleanup passed. Local commit subject: `fix(privacy): refresh local data after operations settle`; the final SHA is recorded in the evidence summary. The one default read-only subagent returned no findings within the bounded review window and was interrupted; all findings above come from direct primary-agent reads.
- Implementation notes: deletion refreshes all affected database caches and the selected onboarding/model consumers; clearing onboarding also refreshes the stored learning goal. Existing Agent-screen invalidation callers retain their WidgetRef API. A test exposed Riverpod 2.6.1's debug dependency check initializing previously unread providers through Ref.invalidate, which started and immediately disposed onboarding. The wrapper now checks Ref.exists for ordinary providers before invalidation; family invalidation stays lazy. A blocked-database fixture verifies that refresh does not start unrelated reads. The initial retry-message check needed to wait for the previous error SnackBar to expire; an added test fixture also had a non-const configuration constructor corrected. These intermediate checks were not counted as passes.
- Full-test recovery: the first full run passed 514 checks but an unchanged readiness initializer CLI test received Dart exit 254 from its build hooks (`File not formatted as yaml: .`) instead of the expected evaluator exit 2. All six related CLI checks passed when rerun; a subsequent full run passed all 515 checks. No test expectation, CLI source or release evidence was changed. Both run results and log hashes are preserved in the evidence summary; the temporary logs have been removed.
- Device preparation: AVD `xunqiu64_api36` started with the established hidden-window/host-GPU mode, launcher PID 27536. Cold-boot package lookup again returned unknown package; reinstalling the previous ordinary APK without launching it exposed the same database SHA-256 `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`. The isolated fixture uses real privacy/profile screens and providers, synthetic in-memory services and a blocked database dependency. The package-lookup cause remains unconfirmed; no AVD data or snapshots were cleared.
- Device results: Android API 36 at 1080x2400/100% and 840x2400/200%, density 420, completed history deletion, content deletion, restore, partial deletion failure/retry and restore rollback at each scale. All 87 observations have equal cached/stored values and zero blocked-database accesses; 44 screenshots are retained. Accepted history deletion and restore finish after returning to the profile, while failure messages preserve the error and retry remains usable. Native file selection and SQLite fault injection are outside this synthetic-service acceptance.
- Separate layout finding: normal-size observations have no framework errors. All large-text errors belong to the unchanged profile: its daily-goal row overflows right by 7.3px and achievement badges overflow bottom by 12px, already present before privacy operations. No privacy/dialog or other framework error was recorded. Final PID 4000 log snapshot has no zone/overflow/error matches, which does not erase the retained profile errors. The controller exited 0 and removed its XML/port forward.
- Restoration: rebuilt and installed the ordinary `lib/main.dart` debug APK (SHA-256 `1ba03645a59333ec327b2119969e3235593e5db249ee28996344f0af2a82f964`), matched its installed hash and verified no `ext.anchor.dl013` in the kernel. Its ordinary entrypoint was not launched. The database hash after restoration matches the pre-fixture baseline; WAL and SHM are absent. Display is restored to 1080x2400/density 420/font scale 1.0. The fixture and emulator are stopped, ADB has no devices, and no matching task runtime remains.
- Evidence/cleanup: retained 44 PNG screenshots, `anchor-dl013-20260909-01a081db-run1-device-evidence.json` and `anchor-dl013-20260909-evidence.json` in the current task visualization directory. Ten selected screenshots were visually inspected, including both confirmation scales and large-text failure/retry/rollback states. Removed exactly six temporary files under the DL-013 prefix after recording their hashes: `ui.dart`, `ui.py`, `full-test.log`, `full-test-rerun.log`, `emulator.out.log` and `emulator.err.log`. Both protected reports retain their baseline hashes and stay untracked; prior evidence, older temporary materials, historical worktrees and dependency/build caches remain retained.

### Completed Leaf DL-014 - Profile large-text readability

- Commit: local commit subject `fix(profile): keep summary and achievements readable`; the final SHA is retained in the evidence summary. All four owned-file hashes matched after commit; tracked files were clean and only the protected reports remained untracked.
- Baseline/input: `315860a`; DL-013's `large-baseline-profile` and `large-history-checkins` screenshots show 7.3px horizontal overflow in the daily-goal row and 12px vertical overflow in achievement tiles at 320 logical pixels/200% text. The unchanged `profile_screen.dart` was read in full; `_buildDailyGoal`, the fixed four-column grid and `_AchievementBadge` constrain these labels. No dedicated profile layout test currently exists.
- Goal/output: show the complete daily-goal and monthly check-in headings/values, completed-goal message and all achievement titles at ordinary size and narrow/large text, with readable descriptions and lock state after tapping a badge. Content grows or wraps within the existing scrollable profile; settings/about actions remain reachable and data/unlock rules remain unchanged.
- Owned files: `lib/features/profile/profile_screen.dart`, new `test/profile_screen_responsive_test.dart`, `docs/CURRENT_STATE.md`, `docs/NEXT_STEPS.md`. Temporary synthetic device helpers/logs and retained evidence use prefix `anchor-dl014-20260909-01a081db-` outside the repository.
- Acceptance: reproduce the observed layout failures on the unchanged screen using real theme/provider consumers and synthetic state. Cover ordinary 390px/100%, 320px/200% and limited height, incomplete/completed goals, locked/unlocked badges, full title/description rendering and navigation/back. Run focused profile/privacy/home checks, full Flutter tests, analysis, formatting and diff checks. Inspect and interact with the actual Android emulator at normal and narrow/200% text; retain screenshots, restore ordinary APK/display values, compare the original database hash, stop the emulator and remove owned temporary files.
- Boundary: presentation changes only; preserve statistics, rewards, unlock thresholds, check-in semantics, provider refresh, routes, strings and storage. Do not broaden this leaf into a profile redesign, data-error redesign, native picker work or release activity.
- Pre-fix reproduction: the unchanged profile passed four of 11 new checks and failed seven. At 320px/200%, the widget font produced horizontal overflow of 220px in the daily-goal header, 144px in the monthly check-in header, 4px in the completed-goal message and 246px in the achievement description. These are widget measurements, separate from the earlier device's 7.3px/12px findings. Include the now-reproduced monthly header in the same owned-file layout scope; preserve its counts and calendar rules.
- Additional reproduction: after fixing the headers/messages, seven checks passed and four still failed because the unchanged grid truncated a title. A single-case rerun identified `连续100天` at 320px/200%. The grid now derives its column count from available width and requested text size; tiles grow with complete titles instead of enforcing an aspect ratio or two-line limit. Headings/counts wrap and descriptions place their lock state below the full text.
- Status: `completed` on 2026-09-10; all 11 profile checks and 26 related privacy/home checks passed (37 total). Full Flutter: 526 passed, exit 0, in 35.1 seconds; analysis found no issues in 67.4 seconds. Two Dart files have unchanged formatting and diff checks pass. The original implementation failed seven of 11 checks, including daily/monthly headers, completed-goal content, achievement descriptions and the fixed grid; the final implementation passes all of them.
- Device acceptance: Android API 36 at 1080x2400/1.0, 840x2400/2.0 and 840x1100/2.0, density 420. Goal completion, all achievement titles, locked/unlocked descriptions, settings/about navigation and back behavior passed. The final fixture run recorded 25 observations, 11 screenshots, zero framework errors, zero blocked database accesses and no unexpected synthetic service calls. Earlier run1-run5 failures were classified as harness-only failures and retained in the evidence; they are not counted as application regressions.
- Restoration/evidence: rebuilt and installed the ordinary `lib/main.dart` debug APK (SHA-256 `4b07f556f27a91e7286ea576d226ea51ee5cd877288507c6562d279e9f28a1bf`), matched the installed hash and verified no `ext.anchor.dl014` in the kernel. The ordinary entrypoint was not launched. The database hash before fixture and after restoration remains `7b5d1f282f695235d10c4a29d93c4022634830a96d9857d6679272c6d612235f`; WAL/SHM are absent. Display is restored to 1080x2400/density 420/font scale 1.0; emulator and matching runtimes are stopped.
- Evidence/cleanup: retained the run6 screenshots and device JSON plus `anchor-dl014-20260909-evidence.json` under the current task visualization directory. Deleted exactly eight DL-014 temporary files after recording hashes: pre-fix/full-test logs, Dart/Python fixtures and two emulator output/error log pairs. Earlier run evidence remains retained for harness classification; protected reports, prior evidence, historical worktrees and dependency/build caches remain untouched.

### Root Decisions And Next Entry

- Round 2 chose and completed DL-002 after DL-001 because the initial analysis found verifiable documentation errors, not missing service wiring.
- Round 3 reproduced and completed DL-003. After its checks, the root returned to the adjacent asynchronous boundaries. Corpus invalidation already refreshes both search providers through their watched corpus; no additional invalidation change was justified by that inspection.
- Round 4 selected the bounded `DL-004` investigation above from the concrete debounce/request-ownership boundary. No unrelated refactor or new product feature is planned.
- Round 5 completed `DL-005`: the metric/tab/context failures are reproduced and fixed, along with zero-size startup constraints and limited-height error scrolling. Its final device and local gates passed; the temporary fixture has been removed and the normal app restored.
- Post-commit root reassessment: `2fb155a` has clean tracked files; only the two protected reports remain untracked. Reviewed the completed search diff and current `NEXT_STEPS.md`, `OPEN_SOURCE_CHECKLIST.md` and release-plan boundaries. All listed repository-local checklist items are complete; no failed scoped test or observed layout defect remains. List-card snippet ellipses are existing previews, not evidence of another header failure. Publication, current-window physical-device/model acceptance and formal cohort work remain external. No speculative new feature, release operation or repeated full test run is justified by this assessment.
- Round 6 root reassessment: reviewed the final quiz diff and completed gates. Source-status filtering, review/mastery formulas and reward amounts are unchanged; all new runtime observations use synthetic data. The remaining local candidates are a bounded check of quiz loading failures/disposal and a separate assessment of partial storage-write failures. Neither candidate is an active implementation or covered by a transactional guarantee from DL-006. No further change to the passing submission/layout implementation is justified by this review.
- Round 7 root reassessment (before DL-008): DL-007 closed the loading failure/disposal candidate. Direct inspection found separate writes for rewards, review, mastery and study records, with check-in and cumulative counters in SharedPreferences. A subsequent isolated failure reproduced duplicate effects on retry and became the already-authorized DL-008 contract above.
- Round 8 root reassessment: DL-008 closes that reproduced partial-write candidate. Reviewed the final persistence/screen/provider, migration, backup and deletion changes against the passing local checks and device evidence. Rechecked `NEXT_STEPS.md`, `OPEN_SOURCE_CHECKLIST.md` and `OPEN_SOURCE_READY.md`; no additional reproducible local defect or incomplete repository-local checklist item was identified. No speculative feature or repeat full test run is justified. The remaining queue is publication authorization, current-window model/physical-device acceptance and formal cohort evidence, so the root enters `waiting_external`.
- Round 9 root reassessment: DL-009 closes the reproduced late-save/old-receipt state gap. The retained large-text home screenshot exposes the existing Today Review heading/count truncation, while direct scheduling-code inspection confirms the separate architecture-document mismatch. Reassess these concrete candidates after the local commit, prioritizing the visible core-flow defect before documentation. No new issue in the passing quiz-state implementation justifies another full run.
- Round 10 root reassessment: DL-010 closes the nonempty review-banner visibility candidate, with normal/large/short device checks and unchanged navigation. The remaining verified local discrepancy is the architecture document's scheduling formulas and save sequence; select a documentation leaf after committing this change. Existing preview ellipses and floating controls over a scrollable body do not justify a new home redesign.
- Round 11 root reassessment: the scoped architecture corrections match implementation. Current release-index reads found a separate ambiguity between their 2026-08-28 development counts / 2026-08-26 signed artifact and current code. Select a small documentation leaf to date those snapshots and link current local results, preserving HOLD, external blockers, APK identity and historical evidence.
- Round 12 root reassessment: reviewed the final evidence-labeling diff and current NEXT_STEPS/release-index queue. All confirmed local candidates from this continuation are complete; the listed repository-local checklist entries are checked. Publication, current-window model/physical-device acceptance and formal cohort work remain external. No new failed scoped check, unverified code change or actionable local finding remains, so the root enters `waiting_external` while retaining its next-entry rules.
- Round 13 entry: the user's direct continuation reopens bounded local analysis at `5c9f1c2`. Inspect privacy backup/restore/deletion and the SQLite-backed statistics introduced by DL-008, including their provider and screen consumers. Prior passing checks are not rerun without a new finding; no new application edit begins before selecting a reproducible issue.
- Round 13 selection: prioritize the observed cache/route-lifetime gaps as DL-013. Reassess the separate picker/confirmation overlap and error boundary after its checks and local commit; do not expand this leaf into storage or release work.
- Round 13 completion: DL-013 closes those cache gaps. The device evidence adds a confirmed profile accessibility defect in `_buildDailyGoal` and the fixed four-column `_AchievementBadge` grid. Prioritize this observed defect during the next root assessment; the picker/confirmation error boundary still needs reproduction before implementation. Preserve the actual profile errors instead of calling the privacy acceptance a global layout pass.
- Round 14 selection: at committed `315860a`, the confirmed profile overflow affects current users and has exact device evidence. Select DL-014 with a focused layout/interaction scope; retain the picker/confirmation candidate for the following root reassessment. No new finding justifies repeating the already passing privacy runtime checks before another code change.
- Round 14 completion: DL-014 closes the profile large-text candidate with widget and real-emulator evidence. The next root pass should reproduce the picker/confirmation concurrency/error boundary before assigning a leaf; do not infer a defect from the earlier harness-only failures.
- Round 15 completion: DL-015 closes the reproduced restore picker ownership and picker-error boundary. The native picker, confirmation and retry paths passed on normal, large and short emulator configurations with synthetic services and a blocked database. Direct review found no separate deletion-dialog defect, so the root enters `waiting_external` while preserving the three Private Alpha HOLD blockers.
- Round 16 completion: DL-016 closes the reproduced project picker ownership and route-exit boundary. The three Android configurations passed with real ZIP/directory DocumentsUI flows, delayed synthetic scanning, retryable errors and a blocked database. The ordinary APK/display/database baseline was restored, the fixture and device ZIPs were removed, and the root resumes analysis for the next local leaf.
- Round 23 completion: recovered and committed DL-022, then reproduced and completed DL-023 at `897dda2`. The final source/test and protected-report hashes match, 577 full Flutter checks pass, both emulator configurations pass, and the ordinary APK/display/database baseline is restored. Adjacent history/detail return callbacks retain mounted guards; no further reproduced local finding was identified in this scoped reassessment. All confirmed candidates from this continuation are closed, so the root enters `waiting_external` for the remaining publication authorization, fresh release evidence and formal cohort work. This is not a claim of exhaustive application auditing or release approval.
- Round 24 completion: continued the unfinished DL-024, resolved the device-discovered library scroll/search failures and committed the accepted code at `414e846`. Reviewed the final scoped diff, 103/605 local results, 50 clean Android observations, all 26 screenshots and the current NEXT_STEPS/release-index queue. The confirmed local recovery/layout candidates are closed. The remaining candidates are publication operations requiring authorization and fresh model/physical-device/cohort evidence; neither can be completed by another synthetic local run. No additional reproduced defect or incomplete local gate was identified, so the root enters `waiting_external`. Both historical worktrees are still clean at their recorded commits and remain retained; no worktree cleanup or remote update was attempted. This bounded review does not claim exhaustive application auditing or release approval.
- Round 25 completion: committed DL-025 at `39fcc57` after 74 focused/613 full checks, clean analysis, 48 Android observations, visual review of all 20 screenshots and verified restoration/cleanup. Reviewed the final list-error diff and current NEXT_STEPS/release indexes: the shared error widget, diagnostic strings, provider invalidation and pending-refresh behavior are unchanged, and no additional failure or incomplete local gate was observed. The remaining candidates are publication operations requiring authorization, fresh model/physical-device acceptance on the release candidate, and formal cohort evidence; another synthetic run cannot complete them. The root enters `waiting_external`. Both historical worktrees remain clean at their recorded commits and are not ancestors of the locally cached `origin/main`; they are retained without remote refresh or cleanup. This bounded reassessment is not an exhaustive application audit or release approval.
- Round 26 completion: committed DL-026 at `5448f8e` after 80 focused/619 full checks, clean analysis, 26 Android observations, visual review of all 20 screenshots and verified restoration/cleanup. Reviewed the final populated-question diff and current NEXT_STEPS/release indexes: filtering, counts, selected-question identity, detail return and provider reads retain their contracts, and no additional failure or incomplete local gate was observed. Existing subtitle previews and off-screen scroll contents do not establish a new defect. The remaining candidates are publication operations requiring authorization, fresh model/physical-device acceptance on the release candidate, and formal cohort evidence; another synthetic run cannot complete them. The root enters `waiting_external`. Both historical worktrees remain clean at their recorded commits and are not ancestors of the locally cached `origin/main`; they are retained without remote refresh or cleanup. This bounded reassessment is not an exhaustive application audit or release approval.
- Round 27 completion: committed DL-027 at `7a022c2` after 71 focused/630 full checks, clean analysis, 30 Android observations, visual review of all 22 final screenshots and verified restoration/cleanup. Reviewed the final bulk-action diff, citation-loader lifetime, post-write mounted boundary, confirmation scrolling/controls and current NEXT_STEPS/release indexes. Both reproduced defects are closed; no additional reproduced failure or incomplete gate was found within this scope. The remaining three candidates are publication authorization, current-window model/physical-device acceptance and formal cohort evidence, so the root enters `waiting_external`. All 78 acceptance-artifact hashes, source/test hashes and protected-report hashes were rechecked. Both historical worktrees remain clean at their recorded commits and are not ancestors of the locally cached `origin/main`; they remain retained without remote refresh or cleanup. The app commit is 91 commits ahead of the locally cached feature remote before this documentation update; no push occurred. Root evidence is `anchor-dl027-20260913-root-review.json` in this task's evidence directory. This bounded reassessment does not claim exhaustive application auditing or release approval.
- Round 28 completion: committed DL-028 at `fa3f274` after 87 focused/646 final full checks, clean analysis, 28 Android observations, visual review of all 24 screenshots and verified restoration/cleanup. Reviewed the accepted individual-save diff, provider-scope lifetime, preserved cache targets, mounted failure/retry and existing learning-plan dependencies, plus current NEXT_STEPS/release indexes. The reproduced stale-cache defect is closed; no further actionable local finding or incomplete gate was identified within this scope. Publication authorization, current-window model/physical-device acceptance and formal cohort evidence remain the three external candidates, so the root enters `waiting_external`. All 58 acceptance-artifact hashes, three application/test hashes, the ordinary APK and both protected-report hashes match; all ten removed temporary files remain absent. The acceptance index is retained as its pre-commit snapshot and this review binds it to `fa3f274`. Both historical worktrees remain clean at their recorded commits and are not ancestors of locally cached `origin/main`; they remain retained. The app commit is 93 commits ahead and zero behind the locally cached feature remote before this documentation update; no fetch or push occurred. No matching task runtime or ADB device is active. The read-only ADB check started its shared server, which was retained. Root evidence is `anchor-dl028-20260913-root-review.json` in this task's evidence directory. Only completion/root documents changed after acceptance; no behavioral gate was rerun. This bounded reassessment does not claim exhaustive application auditing or release approval.
- Round 29 completion: committed DL-029 at `b903c2a` after 95 focused/654 full checks, clean analysis, 32 Android observations, visual review of all 24 final screenshots and verified restoration/cleanup. Reviewed the provider-owned bulk-save completion, exact existing invalidation targets, route-versus-scope lifetime behavior, preserved atomic planning/normalization/error/retry semantics, final widget regressions and current NEXT_STEPS/release indexes. The reproduced stale-cache defect is closed; no additional actionable local finding or incomplete repository-local gate was identified. Publication authorization, current-window model/physical-device acceptance and formal cohort evidence remain the three external candidates, so the root enters `waiting_external`. The final evidence index has 86 artifact hashes (`anchor-dl029-20260914-evidence.json`, SHA-256 `1cc8c93f5cb353498675a4e771921f18b07975bc2b25c3ae279082ceb5dcb0d9`); all three application/test hashes, 24 final screenshot hashes, the ordinary APK, database and protected-report hashes match, and all five removed temporary files remain absent. The acceptance index is retained as its pre-commit snapshot and this review binds it to `b903c2a`. Both historical worktrees remain clean at their recorded commits and are not ancestors of locally cached `origin/main`; they remain retained. The app commit is 95 commits ahead and zero behind the locally cached feature remote before this documentation update; no fetch or push occurred. No matching task runtime, ADB target or emulator remains; shared services were retained. Root evidence is `anchor-dl029-20260914-root-review.json` (SHA-256 `02380ec463011a0889ec2e68ad442718d03f0122215b1fa1fbe7cd7f657c2e38`) in this task's evidence directory. Only completion/root documents changed after acceptance; no behavioral gate was rerun. This bounded reassessment does not claim exhaustive application auditing or release approval.
- Next entry: on a direct task message, evaluate newly authorized work or new actionable evidence, then continue the root after each completed leaf. Automatic wake-ups remain off. Do not repeat DL-001–028 or their completed checks without a new change or failure. The original handoff and narrow heartbeat-format repair were verified; later upstream failures remain outside the current project work.
- Release-only work stays outside this local queue: the documented `HOLD` requires fresh external evidence and the existing release authorization boundary.

### Temporary Materials From This Assessment

- This handoff removed its two temporary `codex-heartbeat-compat-01a07e0b-smoke` output/error files from the system temporary directory and the relay's redundant single-probe `heartbeat-verification.json`, after retaining their validation results and cleanup hashes in `repair-evidence.json`. Retain the compatibility relay, its tests, final live evidence, both verified backups and its running logon task as the repair deliverables. The temporary tool-managed verification server was stopped before the supervised service started. Existing reports, old temporary files and historical worktrees remain untouched.
- Removed this task's `C:\Users\zhang\AppData\Local\Temp\anchor-dl003-20260908-01a07de8-flutter-test.log` after recording its 406-pass result. The automation handoff backup was verified at that time; its path is historical as noted above. No new checkout, worktree, persistent test data or other temporary output was created explicitly by this task; pre-existing build/test caches, screenshots, reports and older temporary files remain untouched.
- Removed this task's `C:\Users\zhang\AppData\Local\Temp\anchor-trellis-20260908-01a07cfd-web-results\.last-run.json` and its empty parent directory after confirming it was the only entry.
- Web tests also write screenshots under `web/test-results/evidence`. Their existence was not inventoried before this run, so retain them; their names, modification times and ignored status do not establish ownership.
- Removed all five new DL-001 temporary files under `C:\Users\zhang\AppData\Local\Temp`: `anchor-loop-dl001-20260908-flutter-test.log`, `anchor-loop-dl001-20260908-helper.py`, `anchor-loop-dl001-20260908-exporter_test.dart`, `anchor-loop-dl001-20260908-fixture.json`, and `anchor-loop-dl001-20260908-inspect.png`. The temporary emulator UI XML was removed after each inspection. The three acceptance screenshots remain as evidence.

## Repository Boundaries

- `D:\Agent\codex\worktrees\anchor-claude-web-productization` is clean at the transition baseline and retained for reference only.
- `D:\Agent\codex\worktrees\anchor-claude-agent-recovery` is clean at `0133321c74d63c53db6afc681fd8422e50659566`. That commit is not an ancestor of the current branch; do not automatically merge or delete it.
- Keep both external task journals and worktrees. No cleanup of pre-existing files is authorized by their age or naming alone.
- Preserve the untracked user reports `docs/TECHNICAL_MODEL_ACCEPTANCE_2026-08-26.md` and `docs/TECHNICAL_MODEL_ACCEPTANCE_2026-08-26_2004.md`; do not stage them.
- Machine-wide Claude installation and settings are not part of this project migration.

## Completed Before The Transition

- `173a6c9`: Agent home header hierarchy.
- `6b08bc3`: Learning home visual hierarchy.
- `f0a637b`: Web gallery integration using actual Android screenshots.
- `eaa748e` and `38177c5`: narrow top bar and large-text layout fixes.

## Previous Codex Leaf

`970eec5`: Agent home learning plan visual hierarchy, implemented directly by Codex.

- Input: the existing workspace snapshot, plan, callbacks, strings, and shared AppColors.
- Output: a quiet white plan surface, readable ink hierarchy, restrained green route accent and primary action, and wrapping at 320 logical pixels / 200% text.
- Implementation scope: `_LearningAgentPlanCard`, `_PlanDetailsDisclosure`, `_PlanMetric`, `_PlanScopeChip`, `_PlanScoreChip`, and `_PlanLoadingCard` in `lib/features/agent/agent_home_screen.dart`; focused layout coverage in the existing Agent widget tests when needed.
- Preserve providers, routes, events, enabled/disabled rules, sessions, storage, and all user-facing strings.
- Acceptance: Agent home navigation and unified workspace tests, Flutter analysis, formatting, full Flutter tests, real emulator screenshots, and `git diff --check`.
- Status: implemented and verified by Codex on 2026-09-05.
- Result: white surface with a 1px neutral border; route title and scope wrap fully; statistics use unframed labels with stronger values; secondary copy has lower weight; the green CTA retains its callbacks and readable disabled colors.
- New regression coverage checks 390px / 100% and 320px / 200% labels and disclosure. The 200% title check failed on the original implementation and passed after the change.

## Previous Evidence Readability Leaf

Agent home expanded plan evidence readability, implemented directly by Codex.

- Input: existing plan evidence, session summary, focus points, strings, and callbacks.
- Output: full wrapping for the Agent Session title, objective, target, source constraint, memory reminder, focus title, and focus reason, including narrow screens and large text.
- Implementation scope: `_AgentSessionSummaryView`, `_AgentSessionRuleRow`, and `_FocusPointRow` in `lib/features/agent/agent_home_screen.dart`; regression coverage in `test/agent_home_navigation_widget_test.dart`.
- No changes to providers, routes, callbacks, strings, state, or storage. Only the one- or two-line truncation limits and title line height changed.
- Status: implemented and verified on 2026-09-05/06. The two new detailed-fixture tests failed before the fix and passed afterward at 390px / 100% and 320px / 200%.
- Coverage includes each expanded label, horizontal bounds, collapse/re-expand, and no checkpoint persistence writes from disclosure.

## Expanded Evidence Verification (2026-09-06)

- `flutter test --no-pub test/agent_home_navigation_widget_test.dart test/learning_agent_unified_workspace_test.dart`: 16 passed.
- `flutter test --no-pub`: 399 passed, zero failures, in the canonical checkout.
- `flutter analyze --no-pub --no-fatal-infos`: no issues found.
- Dart formatting and `git diff --check`: passed.
- Current debug build ran on the real Android 16 / API 36 emulator. Screenshots were inspected at 1080x2400 / 100% text and 840x2400 / 200% text (density 420, 320 logical pixels). Route/scope labels wrap, the primary action remains visible, and expanded evidence titles, constraints, reasons, and metadata wrap without ellipses. App-PID logs contain zero layout-overflow matches.
- Restored and verified emulator size 1080x2400 and font scale 1.0, then shut down the emulator. No matching Flutter run or emulator process remains.
- Screenshots are outside Git under `D:\Agent\codex\visualizations\2026\08\20\01a01e2e-d37b-7760-8934-ae0b4dd72be9`: `anchor-plan-evidence-codex-20260906-normal-details.png` and `anchor-plan-evidence-codex-20260906-large.png`. Previous plan-card screenshots retain their `anchor-plan-codex-20260905-` names.
- Temporary cleanup was blocked by the tool safety policy. These five files created during this leaf remain under `C:\Users\zhang\AppData\Local\Temp`: `anchor-plan-evidence-20260905-inspect.png`, `anchor-plan-evidence-20260905-add.png`, `anchor-plan-evidence-20260906-large-summary.png`, `anchor-plan-evidence-20260906-large-details.png`, and `anchor-plan-evidence-20260905-full-test.log`. They are not source changes or release evidence.

The previous worktree's 393-pass / 2-failure report is historical. Its frozen-hash and secure-storage transform failures did not reproduce in this canonical-checkout run. No frozen evidence or unrelated build files were changed to resolve them.

The debug startup `Zone mismatch` warning was the open reliability candidate for this leaf and is now resolved; the pre-fix reproduction and post-fix startup evidence are recorded below.

## Latest Reliability Leaf

Startup zone alignment, implemented directly by Codex.

- `lib/main.dart` now initializes Flutter bindings inside the same `runZonedGuarded` zone that calls `runApp`, while preserving the existing framework and async error handlers and system UI setup.
- No routes, providers, user-visible strings, storage, credentials, or release settings changed.
- The pre-fix warning was reproduced on the Android debug run. After the change, the real Android 16 / API 36 emulator reported `zoneMismatchLines=0`, `layoutOverflowLines=0`, and `startupErrorLines=0` for Anchor PID 3590.
- The targeted Agent tests, full Flutter test suite, analysis, formatting, and `git diff --check` passed. See Current Verification below for the latest counts. Emulator display settings were left at 1080x2400 / 100% and the emulator was shut down afterward.

## Previous Completed Leaf

`f389cbd`: Agent checkpoint goal and readiness-message readability. Code completed on 2026-09-06; the missing card-specific emulator verification was completed on 2026-09-08.

- The change removes fixed line limits from the goal/tool label and readiness message in `_AgentResumeCheckpointCard`. Existing regression tests cover ordinary text and 320 logical pixels / 200% text. No further application or test code changed during verification.
- The earlier emulator smoke covered startup only. It did not establish checkpoint-card visual acceptance; the results below close that specific gap.
- Reproduction used the existing `_unknownOutcomeCheckpoint(_plan())` fixture from `test/agent_home_navigation_widget_test.dart`, serialized through the production plan codec. A temporary exporter checked that the fixture requires a user decision and contains the long "最终结果尚未保存" message. One uniquely named synthetic checkpoint and one trace were inserted into the emulator's local database; both tables were empty beforehand. This is UI fixture evidence, not a real interrupted import or model-acceptance result.
- On the actual Android 16 / API 36 emulator, the goal, complete readiness message, metadata, and primary button remain visible at 1080x2400 / 100% text and 840x2400 / 200% text, both at density 420. The second configuration is 320 logical pixels wide.
- At 200% text, "确认工具结果" opens the decision dialog and "稍后处理" returns to the unchanged card. Deleting the synthetic session through the card removes it; SQLite `quick_check=ok`, checkpoint count 0, and trace count 0 match the initial state. No source import or AI request was performed.
- That run found `Session` wrapped as `Sessio` / `n` at 320px / 200%. The goal/message fix passed its own checks; the separate header finding was subsequently resolved and verified by DL-001 above.
- Screenshots are outside Git under `D:\Agent\codex\visualizations\2026\09\07\01a07cfd-0714-73c0-bc13-a10f2bdf8784`: `anchor-checkpoint-20260908-normal.png`, `anchor-checkpoint-20260908-large.png`, and `anchor-checkpoint-20260908-large-dialog.png`.

## Checkpoint Body Verification Baseline (2026-09-08)

These dated checks ran against application commit `f389cbd14f116a1be5f98930d4cb8b9a6b3943f8`. The later DL-001 results are recorded above; the original body-verification follow-up changed only two state documents.

- `flutter test --no-pub --reporter expanded test/agent_home_navigation_widget_test.dart test/learning_agent_unified_workspace_test.dart`: 18 passed, zero failures.
- `flutter test --no-pub --reporter expanded`: 401 passed, zero failures, exit code 0.
- `flutter analyze --no-pub --no-fatal-infos`: no issues found. Dart formatting: two files checked, zero changes. `git diff --check`: passed.
- `flutter build apk --debug --no-pub`: passed; the resulting APK was installed on the emulator for the card checks.
- Anchor PID 4455 logs reported `zoneMismatchLines=0`, `layoutOverflowLines=0`, and `frameworkErrorLines=0`. The final check after dialog, deletion, and display restoration also had zero matching framework errors.
- Restored and verified emulator size 1080x2400, density 420, and font scale 1.0. The emulator was shut down; ADB lists no devices and no matching Flutter run or emulator process remains.
- Removed all five temporary files created for this verification: the fixture helper, temporary Dart exporter, synthetic JSON, inspection screenshot, and full-test log. The three acceptance screenshots above are retained.
- The seven temporary files from the previous task remain untouched. Five are listed in the dated expanded-evidence section above; the other two are `C:\Users\zhang\AppData\Local\Temp\anchor-agent-resume-card-20260906-full-test.log` and `C:\Users\zhang\AppData\Local\Temp\anchor-agent-resume-card-20260906-startup.png`. They predate this task and are not current card-acceptance evidence.
- Both untracked user reports retain their initial SHA-256 hashes and are excluded from the documentation commit.

## Previous Header Finding

The `anchor-checkpoint-20260908-large.png` baseline exposed title/phase/delete contention in one row. DL-001 above completed this follow-up, preserving strings, actions, state, storage and goal/message wrapping. Use the continuous-development pointer for the next task; do not reopen this completed finding from its older screenshot.

## Release Boundary

Android identity remains `cc.eu.playlab.anchor`, database `anchor_learning.db`, candidate `1.0.0+2005`. Web remains a separate static demo with local data and no AI provider calls.

The readiness evaluator now returns `GO` for the signed arm64 APK (`641a1a107c687e4903b3c64a65111c4eeff88804d2cc056374026fafa29c54b3`), the connected API 35 physical-device smoke, and the Windhub primary `grok-4.6` plus fallback `glm-5.3-flash` five-case acceptance. A formal cohort study remains optional research and is not synthesized or used as release evidence. Push, deployment and distribution remain outside the current task.
